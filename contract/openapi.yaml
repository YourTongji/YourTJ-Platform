openapi: 3.0.3
info:
  title: YourTJ v2 API
  version: 2.0.0
  description: >
    The single source of truth for the v2 HTTP contract. Client type bindings
    (TS / Swift / Dart) are generated from this file — change the API here first,
    then implement and regenerate. Conventions: bearer JWT auth; money/escrow
    writes use one-time signing intents and require `X-Wallet-Intent`,
    `X-Wallet-Sig`, and the same `Idempotency-Key`; cursor pagination on public
    lists; the error envelope `{ error: { code, message } }`; timestamps are Unix seconds.
servers:
  - url: https://api.yourtj.de/api/v2

tags:
  - { name: auth }
  - { name: identity }
  - { name: activity }
  - { name: wallet }
  - { name: credit }
  - { name: search }
  - { name: courses }
  - { name: selection, description: 选课 (PK) catalogue mirrored from 一系统 }
  - { name: reviews }
  - { name: forum }
  - { name: notifications }
  - { name: platform }
  - { name: media }
  - { name: admin }

security:
  - bearer: []

components:
  securitySchemes:
    bearer: { type: http, scheme: bearer, bearerFormat: JWT }

  parameters:
    Cursor: { name: cursor, in: query, schema: { type: string }, description: Opaque pagination cursor }
    Limit: { name: limit, in: query, schema: { type: integer, default: 20, minimum: 1, maximum: 100 } }
    SelectionCalendarId:
      name: calendarId
      in: query
      required: true
      schema: { type: string, pattern: '^[1-9][0-9]*$' }
      description: Selection calendar that bounds every returned teaching class.
    TeachingClassId:
      name: teachingClassId
      in: path
      required: true
      schema: { type: string, pattern: '^[1-9][0-9]*$' }
      description: Stable upstream teaching-class identifier, not a canonical course code.
    WalletIntent:
      name: X-Wallet-Intent
      in: header
      required: true
      schema: { type: string, format: uuid }
      description: One-time signing intent returned by POST /credit/signing-intents
    WalletSig:
      name: X-Wallet-Sig
      in: header
      required: true
      schema: { type: string }
      description: Base64 Ed25519 signature over the intent's exact signingBytes
    OptionalWalletIntent:
      name: X-Wallet-Intent
      in: header
      required: false
      schema: { type: string, format: uuid }
      description: Required only for the value-moving actions documented on this operation
    OptionalWalletSig:
      name: X-Wallet-Sig
      in: header
      required: false
      schema: { type: string }
      description: Required only for the value-moving actions documented on this operation
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      schema: { type: string, minLength: 1, maxLength: 128 }
    WalletIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema: { type: string, minLength: 1, maxLength: 128 }
    ReconciliationIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema: { type: string, minLength: 8, maxLength: 128, pattern: '^[!-~]+$' }
    AppealIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema: { type: string, minLength: 8, maxLength: 128, pattern: '^[A-Za-z0-9._:-]+$' }
      description: Account-scoped key. Reuse with a different event or reason returns conflict.

  responses:
    BadRequest:   { description: Bad request, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    Unauthorized: { description: Unauthorized, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    Forbidden:    { description: Forbidden, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    RecentAuthRequired: { description: A fresh session-bound authentication is required, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    NotFound:     { description: Not found, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    Conflict:     { description: Conflict, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    RateLimited:  { description: Too many requests, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
    ServiceUnavailable: { description: Upstream service unavailable, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }

  schemas:
    HealthStatus:
      type: object
      required: [status, service, version]
      properties:
        status: { type: string, enum: [ok] }
        service: { type: string }
        version: { type: string }
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            details: { type: object }
    Page:
      type: object
      description: Base cursor-pagination envelope; typed lists override `items`.
      required: [items, nextCursor, hasMore]
      properties:
        items: { type: array, items: {} }
        nextCursor: { type: string, nullable: true }
        hasMore: { type: boolean }

    # ---- identity ----
    Account:
      type: object
      required: [id, handle, avatarUrl, role, capabilities, trustLevel, hasPassword, onboardingRequired, createdAt]
      properties:
        id: { type: string }
        handle: { type: string }
        avatarUrl:
          type: string
          nullable: true
          deprecated: true
          description: Legacy compatibility field; profile images are controlled media assets.
        role: { type: string, enum: [user, mod, admin] }
        capabilities:
          type: array
          items: { type: string }
        trustLevel: { type: integer, minimum: 0, maximum: 6, description: "Unified tea trust level. 0 is visitor UI only; registered accounts are 1–6." }
        hasPassword:
          type: boolean
          description: Owner-only credential state used to choose between first-time password setup and password change.
        onboardingRequired:
          type: boolean
          description: True until the current terms version and required profile/privacy choices are accepted.
        createdAt: { type: integer }
    AccountLifecycleState:
      type: string
      enum: [active, deactivated, deletion_requested, deleted, purged]
    AccountLifecycle:
      type: object
      required: [state, deactivatedAt, deletionRequestedAt, recoverUntil, deletedAt, purgedAt, lifecycleVersion]
      properties:
        state: { $ref: "#/components/schemas/AccountLifecycleState" }
        deactivatedAt: { type: integer, nullable: true }
        deletionRequestedAt: { type: integer, nullable: true }
        recoverUntil: { type: integer, nullable: true }
        deletedAt: { type: integer, nullable: true }
        purgedAt: { type: integer, nullable: true }
        lifecycleVersion: { type: integer, minimum: 1 }
    RecoveryCredential:
      type: object
      description: Purpose-bound credential accepted only by account-recovery routes. It never creates a session or refresh token.
      required: [recoveryToken, expiresAt, lifecycle]
      properties:
        recoveryToken: { type: string, minLength: 43, maxLength: 43 }
        expiresAt: { type: integer }
        lifecycle: { $ref: "#/components/schemas/AccountLifecycle" }
    OnboardingState:
      type: object
      required: [required, currentTermsVersion, acceptedTermsVersion, handle, displayName, bio, profileVisibility, activityVisibility, discoverable, completedAt]
      properties:
        required: { type: boolean }
        currentTermsVersion: { type: string }
        acceptedTermsVersion: { type: string, nullable: true }
        handle: { type: string }
        displayName: { type: string, nullable: true }
        bio: { type: string, nullable: true }
        profileVisibility: { $ref: "#/components/schemas/ProfileVisibility" }
        activityVisibility: { $ref: "#/components/schemas/ActivityVisibility" }
        discoverable: { type: boolean }
        completedAt: { type: integer, nullable: true }
    OnboardingCompleteInput:
      type: object
      additionalProperties: false
      required: [handle, displayName, bio, profileVisibility, activityVisibility, discoverable, acceptedTermsVersion]
      properties:
        handle: { type: string, minLength: 3, maxLength: 30, pattern: '^[a-z0-9._-]+$' }
        displayName: { type: string, nullable: true, minLength: 1, maxLength: 50 }
        bio: { type: string, nullable: true, maxLength: 500 }
        profileVisibility: { $ref: "#/components/schemas/ProfileVisibility" }
        activityVisibility: { $ref: "#/components/schemas/ActivityVisibility" }
        discoverable: { type: boolean }
        acceptedTermsVersion: { type: string, minLength: 1, maxLength: 64 }
    AccountLifecycleMutationInput:
      type: object
      additionalProperties: false
      required: [confirmation]
      properties:
        confirmation: { type: string, enum: [DEACTIVATE, DELETE] }
    DeactivateAccountInput:
      type: object
      additionalProperties: false
      required: [confirmation]
      properties:
        confirmation: { type: string, enum: [DEACTIVATE] }
    DeleteAccountInput:
      type: object
      additionalProperties: false
      required: [confirmation]
      properties:
        confirmation: { type: string, enum: [DELETE] }
    AccountLifecycleMutation:
      type: object
      required: [lifecycle, recovery]
      properties:
        lifecycle: { $ref: "#/components/schemas/AccountLifecycle" }
        recovery: { $ref: "#/components/schemas/RecoveryCredential" }
    DataExportStatus:
      type: string
      enum: [queued, running, ready, failed, expired]
    DataExportJob:
      type: object
      required: [id, status, createdAt, updatedAt, expiresAt, errorCode]
      properties:
        id: { type: string }
        status: { $ref: "#/components/schemas/DataExportStatus" }
        createdAt: { type: integer }
        updatedAt: { type: integer }
        expiresAt: { type: integer }
        errorCode: { type: string, nullable: true }
    DataExportDownloadGrant:
      type: object
      required: [token, expiresAt]
      properties:
        token: { type: string, minLength: 43, maxLength: 43 }
        expiresAt: { type: integer }
    AccountDataExport:
      type: object
      description: Machine-readable owner export assembled through domain-owned projections. Internal evidence, reporter/staff identity, inbound private-message bodies, secrets, and provider metadata are excluded.
      required: [schemaVersion, generatedAt, includedSections, identity, forum, reviews, governance, credit, activity, platform, media]
      properties:
        schemaVersion: { type: string }
        generatedAt: { type: integer }
        includedSections: { type: array, items: { type: string } }
        identity: { type: object }
        forum: { type: object }
        reviews: { type: object }
        governance: { type: object }
        credit: { type: object }
        activity:
          type: object
          description: Owner-visible daily counts, check-ins, current trust projection, and redacted trust transition history.
        platform: { type: object }
        media: { type: array, items: { type: object } }
    ProfileVisibility:
      type: string
      enum: [public, campus, only_me]
    RelationshipListVisibility:
      type: string
      enum: [public, campus, followers, only_me]
    DmPolicy:
      type: string
      description: "following means accounts the recipient follows may start a new conversation."
      enum: [everyone, following, nobody]
    ActivityVisibility:
      type: string
      description: Controls profile activity lists, not the visibility of canonical public forum content.
      enum: [public, campus, only_me]
    MentionPolicy:
      type: string
      description: "following means accounts the recipient follows may create a semantic mention notification."
      enum: [everyone, following, nobody]
    MyProfile:
      type: object
      required: [accountId, displayName, school, bio, website, avatarAssetId, bannerAssetId]
      properties:
        accountId: { type: string }
        displayName: { type: string, nullable: true, minLength: 1, maxLength: 50 }
        school: { type: string, minLength: 1, maxLength: 100 }
        bio: { type: string, nullable: true, maxLength: 500 }
        website: { type: string, format: uri, nullable: true, maxLength: 2048 }
        avatarAssetId: { type: string, nullable: true }
        bannerAssetId: { type: string, nullable: true }
    ProfileUpdateInput:
      type: object
      required: [displayName, bio, website]
      properties:
        displayName: { type: string, nullable: true, minLength: 1, maxLength: 50 }
        school:
          type: string
          minLength: 1
          maxLength: 100
          description: Optional only for rolling compatibility; current clients send the public school label.
        bio: { type: string, nullable: true, maxLength: 500 }
        website:
          type: string
          format: uri
          nullable: true
          maxLength: 2048
          description: HTTPS URLs only.
    ProfilePrivacy:
      type: object
      required: [profileVisibility, activityVisibility, followersVisibility, followingVisibility, discoverable, dmPolicy, mentionPolicy]
      properties:
        profileVisibility: { $ref: "#/components/schemas/ProfileVisibility" }
        activityVisibility: { $ref: "#/components/schemas/ActivityVisibility" }
        followersVisibility: { $ref: "#/components/schemas/RelationshipListVisibility" }
        followingVisibility: { $ref: "#/components/schemas/RelationshipListVisibility" }
        discoverable:
          type: boolean
          description: Controls third-party relationship-list discovery and future account search, not exact-handle navigation.
        dmPolicy: { $ref: "#/components/schemas/DmPolicy" }
        mentionPolicy: { $ref: "#/components/schemas/MentionPolicy" }
    ProfilePrivacyUpdateInput:
      type: object
      required: [profileVisibility, followersVisibility, followingVisibility, discoverable, dmPolicy]
      description: New activity and mention fields are optional only for rolling compatibility with older clients; current clients send both.
      properties:
        profileVisibility: { $ref: "#/components/schemas/ProfileVisibility" }
        activityVisibility: { $ref: "#/components/schemas/ActivityVisibility" }
        followersVisibility: { $ref: "#/components/schemas/RelationshipListVisibility" }
        followingVisibility: { $ref: "#/components/schemas/RelationshipListVisibility" }
        discoverable: { type: boolean }
        dmPolicy: { $ref: "#/components/schemas/DmPolicy" }
        mentionPolicy: { $ref: "#/components/schemas/MentionPolicy" }
    ProfileAssetInput:
      type: object
      required: [assetId]
      properties:
        assetId: { type: string }
    AuthTokens:
      type: object
      required: [accessToken, refreshToken, account]
      properties:
        accessToken: { type: string }
        refreshToken: { type: string }
        account: { $ref: "#/components/schemas/Account" }
    ClientInstallationId:
      type: string
      format: uuid
      pattern: '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-4[0-9a-fA-F]{3}-[89aAbB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$'
      description: Optional first-party UUID v4. The raw value remains on the first-party client; the server stores only an account-scoped digest and replaces the prior active session for the same installation.
    PasswordSetInput:
      type: object
      additionalProperties: false
      required: [newPassword]
      properties:
        newPassword: { type: string, format: password, minLength: 8, maxLength: 128 }
        clientInstallationId: { $ref: "#/components/schemas/ClientInstallationId" }
    PasswordResetInput:
      type: object
      additionalProperties: false
      required: [email, code, newPassword]
      properties:
        email: { type: string, format: email }
        code: { type: string, pattern: '^[0-9]{6}$' }
        newPassword: { type: string, format: password, minLength: 8, maxLength: 128 }
        clientInstallationId: { $ref: "#/components/schemas/ClientInstallationId" }
    PasswordChangeInput:
      type: object
      additionalProperties: false
      required: [currentPassword, newPassword]
      properties:
        currentPassword: { type: string, format: password, minLength: 1, maxLength: 128 }
        newPassword: { type: string, format: password, minLength: 8, maxLength: 128 }
        clientInstallationId: { $ref: "#/components/schemas/ClientInstallationId" }
    EmailCodePurpose:
      type: string
      enum: [login, registration, appeal, recovery]
    EmailCodeRequest:
      type: object
      required: [email, captchaToken]
      properties:
        email: { type: string, format: email }
        captchaToken: { type: string, minLength: 1 }
        purpose:
          allOf: [{ $ref: "#/components/schemas/EmailCodePurpose" }]
          description: Optional only for rolling compatibility. Appeal and recovery codes are consumed only by their purpose-bound routes.
    EmailCodeVerification:
      type: object
      required: [email, code]
      properties:
        email: { type: string, format: email }
        code: { type: string, pattern: '^[0-9]{6}$' }
        purpose:
          allOf: [{ $ref: "#/components/schemas/EmailCodePurpose" }]
          description: Optional only for rolling compatibility. This full-login endpoint rejects appeal- and recovery-purpose codes.
        handle: { type: string, minLength: 3, maxLength: 30 }
        password: { type: string, format: password, minLength: 8, maxLength: 128 }
        clientInstallationId: { $ref: "#/components/schemas/ClientInstallationId" }
    RefreshInput:
      type: object
      required: [refreshToken]
      properties:
        refreshToken: { type: string, minLength: 1, maxLength: 256 }
    Session:
      type: object
      required: [id, isCurrent, createdAt, lastUsedAt, expiresAt]
      properties:
        id: { type: string }
        isCurrent: { type: boolean }
        deviceLabel: { type: string, nullable: true, maxLength: 200 }
        createdAt: { type: integer }
        lastUsedAt: { type: integer }
        expiresAt: { type: integer }
    SessionPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - type: object
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/Session" } }
    RecentAuthMethod:
      type: string
      enum: [password, email_code]
    RecentAuthStatus:
      type: object
      required: [sessionBound, isFresh, authenticatedAt, expiresAt, method, availableMethods]
      properties:
        sessionBound:
          type: boolean
          description: False for rolling-window legacy JWTs; high-risk mutations fail closed until a new session login.
        isFresh: { type: boolean }
        authenticatedAt: { type: integer, nullable: true }
        expiresAt: { type: integer, nullable: true }
        method:
          allOf: [{ $ref: "#/components/schemas/RecentAuthMethod" }]
          nullable: true
        availableMethods:
          type: array
          items: { $ref: "#/components/schemas/RecentAuthMethod" }
          description: Password appears only when the authenticated account has one; email_code never accepts a client-supplied email.
    RecentAuthVerifyInput:
      type: object
      additionalProperties: false
      required: [method]
      properties:
        method: { $ref: "#/components/schemas/RecentAuthMethod" }
        password: { type: string, format: password, minLength: 1, maxLength: 128 }
        code: { type: string, pattern: '^[0-9]{6}$' }
      description: Exactly the credential matching method must be supplied; the server binds success to the current revocable session.
    AppealAccessToken:
      type: object
      required: [accessToken, expiresAt]
      properties:
        accessToken:
          type: string
          description: Short-lived bearer accepted only by /me/appeals and /me/governance-notices.
        expiresAt: { type: integer }
    AppealEmailVerification:
      type: object
      additionalProperties: false
      required: [email, code]
      properties:
        email: { type: string, format: email }
        code: { type: string, pattern: '^[0-9]{6}$' }
    AppealStatus:
      type: string
      enum: [submitted, in_review, upheld, overturned, amended, withdrawn]
    AppealHistory:
      type: object
      required: [id, toStatus, reason, createdAt]
      properties:
        id: { type: string }
        fromStatus: { allOf: [{ $ref: "#/components/schemas/AppealStatus" }], nullable: true }
        toStatus: { $ref: "#/components/schemas/AppealStatus" }
        reason: { type: string, minLength: 3, maxLength: 1000 }
        metadata:
          type: object
          nullable: true
          description: Bounded public decision metadata; currently only a shortened sanction endsAt.
        createdAt: { type: integer }
    Appeal:
      type: object
      required: [id, governanceEventId, originalAction, targetKind, targetId, dispositionKind, status, submissionReason, submittedAt, appealableUntil, version, history]
      properties:
        id: { type: string }
        governanceEventId: { type: string }
        originalAction: { type: string }
        originalReason:
          type: string
          nullable: true
          maxLength: 1000
          description: Safe disposition summary for the owner; authorized staff may receive the internal reason.
        targetKind: { type: string, enum: [sanction, forum_thread, forum_comment, review] }
        targetId: { type: string }
        dispositionKind: { type: string, enum: [silence, suspend, hide, delete] }
        status: { $ref: "#/components/schemas/AppealStatus" }
        submissionReason: { type: string, minLength: 3, maxLength: 1000 }
        submittedAt: { type: integer }
        appealableUntil: { type: integer }
        reviewStartedAt: { type: integer, nullable: true }
        decisionReason: { type: string, nullable: true, minLength: 3, maxLength: 1000 }
        amendment: { type: object, nullable: true }
        decidedAt: { type: integer, nullable: true }
        version: { type: integer, minimum: 1 }
        history: { type: array, items: { $ref: "#/components/schemas/AppealHistory" } }
    AdminAppeal:
      allOf:
        - $ref: "#/components/schemas/Appeal"
        - type: object
          required: [appellantAccountId]
          properties:
            appellantAccountId: { type: string }
            reviewerAccountId: { type: string, nullable: true }
    SubmitAppealInput:
      type: object
      additionalProperties: false
      required: [governanceEventId, reason]
      properties:
        governanceEventId: { type: string }
        reason: { type: string, minLength: 3, maxLength: 1000 }
    AppealTransitionInput:
      type: object
      additionalProperties: false
      required: [expectedVersion, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        reason: { type: string, minLength: 3, maxLength: 1000 }
    AppealDecisionInput:
      type: object
      additionalProperties: false
      required: [expectedVersion, outcome, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        outcome: { type: string, enum: [upheld, overturned, amended] }
        reason: { type: string, minLength: 3, maxLength: 1000 }
        amendedEndsAt:
          type: integer
          nullable: true
          description: Only for amended sanctions; must shorten the active sanction.
    GovernanceNotice:
      type: object
      required: [id, noticeType, subjectKind, subjectId, summary, targetUrl, read, createdAt]
      properties:
        id: { type: string }
        noticeType:
          type: string
          enum: [sanction_applied, content_restricted, appeal_submitted, appeal_in_review, appeal_upheld, appeal_overturned, appeal_amended, appeal_withdrawn]
        subjectKind: { type: string, enum: [sanction, forum_thread, forum_comment, review, appeal] }
        subjectId: { type: string }
        summary: { type: string, minLength: 1, maxLength: 500 }
        appealId: { type: string, nullable: true }
        targetUrl: { type: string, pattern: '^/' }
        read: { type: boolean }
        readAt: { type: integer, nullable: true }
        createdAt: { type: integer }
    GovernanceNoticeReadInput:
      type: object
      additionalProperties: false
      properties:
        ids: { type: array, minItems: 1, maxItems: 100, items: { type: string } }
        all: { type: boolean }

    # ---- courses / reviews ----
    Department: { type: object, properties: { id: { type: string }, name: { type: string } } }
    Teacher:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        title: { type: string, nullable: true }
        department: { type: string, nullable: true }
    Course:
      type: object
      properties:
        id: { type: string }
        code: { type: string }
        name: { type: string }
        credit: { type: number, nullable: true }
        department: { type: string, nullable: true }
        teacherName: { type: string, nullable: true }
        reviewCount: { type: integer }
        reviewAvg: { type: number, nullable: true }
    CourseDetail:
      allOf:
        - $ref: "#/components/schemas/Course"
        - type: object
          properties:
            teachers: { type: array, items: { $ref: "#/components/schemas/Teacher" } }
            aliases: { type: array, items: { type: string } }
    Review:
      type: object
      required: [viewerLiked, canEdit, canReport]
      properties:
        id: { type: string }
        courseId: { type: string }
        rating: { type: integer }
        comment: { type: string, nullable: true }
        score: { type: string, nullable: true }
        semester: { type: string, nullable: true }
        authorHandle: { type: string }
        authorAvatar: { type: string, nullable: true, description: Reserved for a current platform-owned avatar projection. Legacy reviewer-provided remote URLs are never returned; null until Reviews integrates a typed Media projection. }
        approveCount: { type: integer }
        viewerLiked: { type: boolean, description: Whether the authenticated viewer currently likes this review; false for anonymous viewers. }
        canEdit: { type: boolean, description: Server-authoritative permission for the current viewer to edit this review. }
        canReport: { type: boolean, description: Server-authoritative permission for the current viewer to report this review. }
        status: { type: string, enum: [visible, hidden, pending] }
        createdAt: { type: integer }
    ReviewInput:
      type: object
      required: [rating]
      properties:
        rating: { type: integer, minimum: 0, maximum: 5 }
        comment: { type: string }
        semester: { type: string }
        score: { type: string }
    CreateReviewInput:
      allOf:
        - $ref: "#/components/schemas/ReviewInput"
        - type: object
          required: [captchaToken]
          properties:
            captchaToken: { type: string, minLength: 1 }
    AiSummary:
      type: object
      properties:
        courseId: { type: string }
        summary: { type: string }
        model: { type: string }
        updatedAt: { type: integer }
    Report:
      type: object
      required: [id, reviewId, reason, status, createdAt]
      properties:
        id: { type: string }
        reviewId: { type: string }
        reason: { type: string }
        status: { type: string, enum: [open, upheld, rejected, ignored] }
        courseId: { type: string, nullable: true }
        reviewAuthorHandle: { type: string, nullable: true }
        reviewRating: { type: integer, minimum: 0, maximum: 5, nullable: true }
        reviewStatus: { type: string, enum: [visible, hidden, pending], nullable: true }
        reviewExcerpt: { type: string, nullable: true }
        createdAt: { type: integer }
    ReviewReportResolutionInput:
      type: object
      required: [action, note]
      properties:
        action: { type: string, enum: [uphold, reject, ignore] }
        note: { type: string, minLength: 3, maxLength: 1000 }

    # ---- wallet / credit (Web2.5) ----
    Wallet: { type: object, properties: { accountId: { type: string }, balance: { type: integer } } }
    WalletClaimChallenge:
      type: object
      required: [challengeId, nonce]
      properties:
        challengeId: { type: string }
        nonce: { type: string }
    LedgerEntry:
      type: object
      properties:
        seq: { type: integer }
        txId: { type: string }
        type: { type: string, enum: [mint, tip, escrow_hold, escrow_release] }
        fromAccount: { type: string, nullable: true }
        toAccount: { type: string, nullable: true }
        amount: { type: integer }
        hash: { type: string }
        createdAt: { type: integer }
    LedgerVerify:
      type: object
      properties:
        ok: { type: boolean }
        latestSeq: { type: integer }
        latestHash: { type: string }
    ReconciliationRunInput:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
    CreditReconciliationRun:
      type: object
      required: [id, status, requestedBy, reason, walletsChecked, driftedWallets, missingWallets, balanceDriftedWallets, sequenceDriftedWallets, totalAbsoluteDrift, createdAt]
      properties:
        id: { type: string, format: uuid }
        status: { type: string, enum: [queued, running, succeeded, failed] }
        requestedBy: { type: string }
        reason: { type: string }
        ledgerOk: { type: boolean, nullable: true }
        ledgerLatestSeq: { type: integer, format: int64, nullable: true }
        ledgerLatestHash: { type: string, nullable: true }
        ledgerFailureSeq: { type: integer, format: int64, nullable: true }
        walletsChecked: { type: integer, format: int64, minimum: 0 }
        driftedWallets: { type: integer, format: int64, minimum: 0 }
        missingWallets: { type: integer, format: int64, minimum: 0 }
        balanceDriftedWallets: { type: integer, format: int64, minimum: 0 }
        sequenceDriftedWallets: { type: integer, format: int64, minimum: 0 }
        totalAbsoluteDrift: { type: string, pattern: '^[0-9]+$' }
        errorCode: { type: string, nullable: true }
        createdAt: { type: integer, format: int64 }
        startedAt: { type: integer, format: int64, nullable: true }
        completedAt: { type: integer, format: int64, nullable: true }
    CreditReconciliationWallet:
      type: object
      required: [accountId, expectedBalance, delta, expectedLastSeq, walletExists, hasBalanceDrift, hasSequenceDrift]
      properties:
        accountId: { type: string }
        expectedBalance: { type: string, pattern: '^-?[0-9]+$' }
        actualBalance: { type: string, pattern: '^-?[0-9]+$', nullable: true }
        delta: { type: string, pattern: '^-?[0-9]+$' }
        expectedLastSeq: { type: integer, format: int64, minimum: 0 }
        actualLastSeq: { type: integer, format: int64, minimum: 0, nullable: true }
        walletExists: { type: boolean }
        hasBalanceDrift: { type: boolean }
        hasSequenceDrift: { type: boolean }
    CreditReconciliationStats:
      type: object
      required: [totalRuns, failedRuns, ledgerFailureRuns, runsWithDrift]
      properties:
        totalRuns: { type: integer, format: int64, minimum: 0 }
        failedRuns: { type: integer, format: int64, minimum: 0 }
        ledgerFailureRuns: { type: integer, format: int64, minimum: 0 }
        runsWithDrift: { type: integer, format: int64, minimum: 0 }
        latestRun:
          allOf: [{ $ref: "#/components/schemas/CreditReconciliationRun" }]
          nullable: true
    SigningIntentInput:
      type: object
      required: [action, request]
      properties:
        action:
          type: string
          enum: [credit.tip, credit.task.create, credit.task.action, credit.product.purchase, credit.purchase.action]
        request: { type: object }
    SigningIntent:
      type: object
      required: [intentId, signingBytes, expiresAt]
      properties:
        intentId: { type: string, format: uuid }
        signingBytes: { type: string }
        expiresAt: { type: integer, format: int64 }
    TipInput:
      type: object
      required: [toAccountId, amount, targetType, targetId]
      description: The recipient must be the active author of the visible target; self-tips are rejected.
      properties:
        toAccountId: { type: string }
        amount: { type: integer, minimum: 1 }
        targetType: { type: string, enum: [review, thread, comment] }
        targetId: { type: string }
    Task:
      type: object
      properties:
        id: { type: string }
        creatorId: { type: string }
        acceptorId: { type: string, nullable: true }
        title: { type: string }
        description: { type: string, nullable: true }
        rewardAmount: { type: integer }
        contactInfo: { type: string, nullable: true, description: Visible only to controlled parties }
        status: { type: string, enum: [open, in_progress, submitted, completed, cancelled] }
        createdAt: { type: integer }
    TaskInput:
      type: object
      required: [title, rewardAmount]
      properties:
        title: { type: string }
        description: { type: string }
        rewardAmount: { type: integer, minimum: 1 }
        contactInfo: { type: string }
    TaskAction:
      type: object
      required: [action]
      properties:
        action: { type: string, enum: [submit, confirm, cancel, reject, delete] }
    Product:
      type: object
      description: Public listing data. Private delivery instructions are never exposed here.
      properties:
        id: { type: string }
        sellerId: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        price: { type: integer }
        stock: { type: integer }
        status: { type: string, enum: [on_sale, off_sale, sold_out] }
        createdAt: { type: integer }
    ProductInput:
      type: object
      required: [title, price, stock]
      properties:
        title: { type: string }
        description: { type: string }
        price: { type: integer, minimum: 1 }
        stock: { type: integer, minimum: 0 }
        deliveryInfo: { type: string }
    Purchase:
      type: object
      properties:
        id: { type: string }
        productId: { type: string }
        buyerId: { type: string }
        sellerId: { type: string }
        amount: { type: integer }
        status: { type: string, enum: [pending, accepted, delivered, completed, cancelled] }
        deliveryInfo: { type: string, nullable: true, description: Visible only to the purchase buyer and seller }
        createdAt: { type: integer }
    PurchaseAction:
      type: object
      required: [action]
      properties:
        action: { type: string, enum: [accept, deliver, confirm, cancel] }

    # ---- search ----
    CourseSearchHit:
      type: object
      required: [id, code, name, credit, department, teacherName, reviewCount, reviewAvg]
      properties:
        id: { type: string }
        code: { type: string }
        name: { type: string }
        credit: { type: number, nullable: true }
        department: { type: string, nullable: true }
        teacherName: { type: string, nullable: true }
        reviewCount: { type: integer, minimum: 0 }
        reviewAvg: { type: number, nullable: true }
    ReviewSearchHit:
      type: object
      required: [id, courseId, courseName, rating, comment, approveCount, createdAt]
      properties:
        id: { type: string }
        courseId: { type: string }
        courseName: { type: string }
        rating: { type: integer, minimum: 0, maximum: 5 }
        comment: { type: string, nullable: true }
        approveCount: { type: integer, minimum: 0 }
        createdAt: { type: integer }
    ThreadSearchHit:
      type: object
      required: [id, title, bodyExcerpt, board, tags, authorHandle, replyCount, voteCount, createdAt, status]
      properties:
        id: { type: string }
        title: { type: string }
        bodyExcerpt: { type: string }
        board: { type: string }
        tags: { type: array, items: { type: string } }
        authorHandle: { type: string }
        replyCount: { type: integer, minimum: 0 }
        voteCount: { type: integer }
        createdAt: { type: integer }
        status: { type: string, enum: [visible] }
    UserSearchHit:
      type: object
      required: [id, handle, displayName, avatarUrl, role, followerCount, following]
      properties:
        id: { type: string }
        handle: { type: string }
        displayName: { type: string, nullable: true }
        avatarUrl: { type: string, nullable: true, description: Short-lived clean thumb_256 compatibility URL; refresh the owning search response after expiry. }
        role: { type: string, enum: [user, mod, admin] }
        followerCount: { type: integer, minimum: 0 }
        following: { type: boolean }
    BoardSearchHit:
      type: object
      required: [id, slug, name, description, threadCount]
      properties:
        id: { type: string }
        slug: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        threadCount: { type: integer, minimum: 0 }
    TagSearchHit:
      type: object
      required: [id, slug, name, description, threadCount]
      properties:
        id: { type: string }
        slug: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        threadCount: { type: integer, minimum: 0 }
    SearchResultScope:
      type: string
      enum: [course, review, thread, user, board, tag]
    SearchHighlightRange:
      type: object
      required: [start, end]
      description: Half-open Unicode character offsets into the named canonical field; clients render text nodes and never HTML.
      properties:
        start: { type: integer, minimum: 0 }
        end: { type: integer, minimum: 1 }
    SearchHighlight:
      type: object
      required: [scope, id, field, ranges]
      description: Presentation metadata computed only after the owning domain rehydrates and authorizes the result.
      properties:
        scope: { $ref: "#/components/schemas/SearchResultScope" }
        id: { type: string }
        field: { type: string, enum: [name, code, teacherName, department, courseName, comment, title, bodyExcerpt, board, authorHandle, handle, displayName, slug, description] }
        ranges:
          type: array
          maxItems: 8
          items: { $ref: "#/components/schemas/SearchHighlightRange" }
    SearchResult:
      type: object
      required: [courses, reviews, threads, users, boards, tags, nextCursor, hasMore, hasMoreScopes, failedScopes, highlights, suggestedQuery]
      properties:
        courses: { type: array, items: { $ref: "#/components/schemas/CourseSearchHit" } }
        reviews: { type: array, items: { $ref: "#/components/schemas/ReviewSearchHit" } }
        threads: { type: array, items: { $ref: "#/components/schemas/ThreadSearchHit" } }
        users: { type: array, items: { $ref: "#/components/schemas/UserSearchHit" } }
        boards: { type: array, items: { $ref: "#/components/schemas/BoardSearchHit" } }
        tags: { type: array, items: { $ref: "#/components/schemas/TagSearchHit" } }
        nextCursor:
          type: string
          nullable: true
          description: Opaque continuation for a specific non-all type, bound to the normalized query and type.
        hasMore: { type: boolean }
        hasMoreScopes:
          type: array
          items: { $ref: "#/components/schemas/SearchResultScope" }
          description: Sections with more privacy-revalidated results; all-scope clients use this for "see more" links.
        failedScopes:
          type: array
          items: { $ref: "#/components/schemas/SearchResultScope" }
          description: Included sections that failed while other sections remained usable. Never contains internal errors.
        highlights:
          type: array
          items: { $ref: "#/components/schemas/SearchHighlight" }
          description: Safe ranges over the returned canonical fields; never snippets or markup copied from the search index.
        suggestedQuery:
          type: string
          nullable: true
          description: Conservative spelling suggestion inferred only from words in the visible, owner-rehydrated result set; null when ambiguous.

    # ---- selection (选课/PK) ----
    Calendar: { type: object, properties: { id: { type: string }, name: { type: string }, isCurrent: { type: boolean } } }
    Campus: { type: object, properties: { id: { type: string }, name: { type: string } } }
    Faculty: { type: object, properties: { id: { type: string }, name: { type: string }, campusId: { type: string, nullable: true } } }
    Major:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        facultyId: { type: string, nullable: true }
        grade: { type: string, nullable: true }
    CourseNature: { type: object, properties: { id: { type: string }, name: { type: string } } }
    SelectionCourse:
      type: object
      description: One teaching class in the selection mirror. Multiple teaching classes may share a course code.
      required: [id, code, name, credit, natureId, calendarId, campusId, teacherName, teacherNames]
      properties:
        id: { type: string, description: Stable upstream teaching-class identifier. }
        code: { type: string, description: Canonical course code; not unique across teaching classes or calendars. }
        name: { type: string }
        credit: { type: number, nullable: true }
        natureId: { type: string, nullable: true }
        calendarId: { type: string, nullable: true }
        campusId: { type: string, nullable: true }
        teacherName: { type: string, nullable: true }
        teacherNames: { type: array, items: { type: string } }
    TimeSlot:
      type: object
      required: [courseId, teacherName, weekday, startSlot, endSlot, weeks, location]
      properties:
        courseId: { type: string, description: Stable teaching-class identifier matching SelectionCourse.id. }
        teacherName: { type: string, nullable: true }
        weekday: { type: integer }
        startSlot: { type: integer }
        endSlot: { type: integer }
        weeks: { type: string, nullable: true }
        location: { type: string, nullable: true }
    LatestUpdate:
      type: object
      required: [updatedAt]
      properties:
        updatedAt: { type: string, format: date-time, nullable: true }

    # ---- forum (F1) ----
    Board:
      type: object
      required: [id, slug, name, parentId, description, position, isLocked, minTrustToPost, isQa, threadCount, canPost, postingRestriction]
      properties:
        id: { type: string }
        slug: { type: string }
        name: { type: string }
        parentId: { type: string, nullable: true }
        description: { type: string, nullable: true }
        position: { type: integer }
        isLocked: { type: boolean }
        minTrustToPost: { type: integer, minimum: 1, maximum: 6 }
        isQa: { type: boolean }
        threadCount: { type: integer, minimum: 0 }
        canPost: { type: boolean }
        postingRestriction:
          type: string
          enum: [login_required, board_locked, trust_level]
          nullable: true
    Tag: { type: object, properties: { id: { type: string }, slug: { type: string }, name: { type: string }, description: { type: string, nullable: true }, threadCount: { type: integer }, createdAt: { type: integer } } }
    ContentFormat:
      type: string
      enum: [plain_v1, markdown_v1]
      default: plain_v1
    ForumAttachment:
      type: object
      description: Minimal clean Media projection for one server-validated yourtj-asset Markdown reference. The canonical destination is never a fetch URL.
      required: [assetId, reference, position, alt, url, expiresAt, width, height]
      properties:
        assetId: { type: string }
        reference: { type: string, pattern: '^yourtj-asset:[1-9][0-9]*$' }
        position: { type: integer, minimum: 0, maximum: 7 }
        alt: { type: string, minLength: 1, maxLength: 300 }
        url: { type: string, format: uri, description: Authorization-derived clean image URL; never persisted in Forum content. }
        expiresAt: { type: integer, description: Unix seconds; refetch the owning Forum resource before or after expiry rather than calling an owner-only Media route. }
        width: { type: integer, minimum: 1, maximum: 1280, nullable: true }
        height: { type: integer, minimum: 1, maximum: 1280, nullable: true }
    Thread:
      type: object
      required: [id, boardId, authorHandle, authorAvatar, title, bodyExcerpt, contentVersion, replyCount, voteCount, hotScore, status, createdAt, lastActivityAt, tags, attachments, viewerVote, isBookmarked, canEdit, canDelete, canModerate]
      properties:
        id: { type: string }
        boardId: { type: string }
        authorHandle: { type: string }
        authorDisplayName: { type: string, nullable: true }
        authorAvatar:
          allOf: [{ $ref: "#/components/schemas/MediaDelivery" }]
          nullable: true
          description: Current short-lived clean thumb_256 avatar projection for the active author; null when no publishable avatar is available.
        title: { type: string }
        bodyExcerpt: { type: string, nullable: true }
        contentVersion: { type: integer, format: int64, minimum: 1 }
        replyCount: { type: integer }
        voteCount: { type: integer }
        hotScore: { type: number, nullable: true }
        status: { type: string, enum: [visible] }
        createdAt: { type: integer }
        lastActivityAt: { type: integer }
        tags: { type: array, items: { type: string } }
        attachments: { type: array, maxItems: 1, description: At most the first clean image for a bounded feed card., items: { $ref: "#/components/schemas/ForumAttachment" } }
        viewerVote: { type: string, enum: [up, down], nullable: true }
        isBookmarked: { type: boolean }
        canEdit: { type: boolean, description: Server-authoritative author edit permission for this viewer. }
        canDelete: { type: boolean, description: Server-authoritative author delete permission for this viewer. }
        canModerate: { type: boolean, description: Server-authoritative moderation permission including role hierarchy. }
    ThreadFeed:
      allOf: [{ $ref: "#/components/schemas/Thread" }, { type: object, properties: { unreadCount: { type: integer, nullable: true } } }]
    ThreadDetail:
      type: object
      required: [id, boardId, authorHandle, authorAvatar, authorId, title, body, contentFormat, contentVersion, replyCount, voteCount, hotScore, tags, attachments, status, pinnedAt, pinnedGlobally, featuredAt, closedAt, archivedAt, deletedAt, editedAt, hiddenAt, createdAt, lastActivityAt, solvedAnswerId, viewerVote, isBookmarked, myLastReadCommentId, mySubscriptionLevel, poll, canEdit, canDelete, canModerate]
      properties:
        id: { type: string }
        boardId: { type: string }
        authorHandle: { type: string }
        authorDisplayName: { type: string, nullable: true }
        authorAvatar:
          allOf: [{ $ref: "#/components/schemas/MediaDelivery" }]
          nullable: true
          description: Current short-lived clean thumb_256 avatar projection for the active author; null when no publishable avatar is available.
        authorId: { type: string }
        title: { type: string }
        body: { type: string, nullable: true }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        contentVersion: { type: integer, format: int64, minimum: 1 }
        replyCount: { type: integer }
        voteCount: { type: integer }
        hotScore: { type: number, nullable: true }
        tags: { type: array, items: { type: string } }
        attachments: { type: array, maxItems: 8, items: { $ref: "#/components/schemas/ForumAttachment" } }
        status: { type: string }
        pinnedAt: { type: integer, nullable: true }
        pinnedGlobally: { type: boolean }
        featuredAt: { type: integer, nullable: true }
        closedAt: { type: integer, nullable: true }
        archivedAt: { type: integer, nullable: true }
        deletedAt: { type: integer, nullable: true }
        editedAt: { type: integer, nullable: true }
        hiddenAt: { type: integer, nullable: true }
        createdAt: { type: integer }
        lastActivityAt: { type: integer }
        solvedAnswerId: { type: string, nullable: true }
        viewerVote: { type: string, enum: [up, down], nullable: true }
        isBookmarked: { type: boolean }
        myLastReadCommentId: { type: string, nullable: true }
        mySubscriptionLevel: { type: string, enum: [watching, tracking, muted], nullable: true }
        poll:
          allOf: [{ $ref: "#/components/schemas/Poll" }]
          nullable: true
        canEdit: { type: boolean, description: Server-authoritative author edit permission for this viewer. }
        canDelete: { type: boolean, description: Server-authoritative author delete permission for this viewer. }
        canModerate: { type: boolean, description: Server-authoritative moderation permission including role hierarchy. }
    ThreadInput:
      type: object
      required: [boardId, title]
      properties:
        boardId: { type: string }
        title: { type: string, minLength: 1, maxLength: 200 }
        body: { type: string, nullable: true, maxLength: 50000 }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        tags: { type: array, items: { type: string, minLength: 1, maxLength: 64 }, maxItems: 3, uniqueItems: true }
        attachmentAssetIds:
          type: array
          maxItems: 8
          uniqueItems: true
          description: Ordered asset ids that must exactly equal the markdown_v1 yourtj-asset image destinations. Must be empty for plain_v1.
          items: { type: string, pattern: '^[1-9][0-9]*$' }
        poll: { $ref: "#/components/schemas/PollInput" }
    ThreadUpdateInput:
      type: object
      properties:
        expectedVersion:
          type: integer
          format: int64
          minimum: 1
          default: 1
          description: Compare-and-swap version. Legacy omission is treated as version 1 and conflicts once content has changed.
        title: { type: string, minLength: 1, maxLength: 200 }
        body: { type: string, maxLength: 50000 }
        contentFormat:
          allOf: [{ $ref: "#/components/schemas/ContentFormat" }]
          description: Required whenever body is supplied; legacy omission is treated as plain_v1.
        tags: { type: array, items: { type: string, minLength: 1, maxLength: 64 }, maxItems: 3, uniqueItems: true }
        attachmentAssetIds:
          type: array
          maxItems: 8
          uniqueItems: true
          description: When body is supplied, ordered ids must exactly match every yourtj-asset image reference; otherwise omit or send an empty array.
          items: { type: string, pattern: '^[1-9][0-9]*$' }
    ThreadPinInput: { type: object, properties: { globally: { type: boolean } } }
    ThreadMoveInput: { type: object, required: [boardId], properties: { boardId: { type: string } } }
    AdminThreadActionInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
        globally: { type: boolean }
        boardId: { type: string }
    AdminCommentActionInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
    PostRevision:
      type: object
      required: [id, seq, editorId, oldTitle, oldBody, oldContentFormat, oldContentVersion, attachments, createdAt]
      properties:
        id: { type: string }
        seq: { type: integer }
        editorId: { type: string }
        oldTitle: { type: string, nullable: true }
        oldBody: { type: string }
        oldContentFormat: { $ref: "#/components/schemas/ContentFormat" }
        oldContentVersion: { type: integer, format: int64, minimum: 1 }
        attachments: { type: array, maxItems: 8, items: { $ref: "#/components/schemas/ForumAttachment" } }
        createdAt: { type: integer }
    Comment:
      type: object
      required: [id, threadId, parentId, path, authorHandle, authorAvatar, authorId, body, contentFormat, contentVersion, attachments, voteCount, viewerVote, isBookmarked, isDeleted, isHidden, editedAt, createdAt, quotedCommentId, isSolved, canEdit, canDelete, canModerate]
      properties:
        id: { type: string }
        threadId: { type: string }
        parentId: { type: string, nullable: true }
        path: { type: string }
        authorHandle: { type: string }
        authorDisplayName: { type: string, nullable: true }
        authorAvatar:
          allOf: [{ $ref: "#/components/schemas/MediaDelivery" }]
          nullable: true
          description: Current short-lived clean thumb_256 avatar projection for the active author; null when no publishable avatar is available.
        authorId: { type: string }
        body: { type: string }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        contentVersion: { type: integer, format: int64, minimum: 1 }
        attachments: { type: array, maxItems: 4, items: { $ref: "#/components/schemas/ForumAttachment" } }
        voteCount: { type: integer }
        viewerVote: { type: string, enum: [up, down], nullable: true }
        isBookmarked: { type: boolean }
        isDeleted: { type: boolean }
        isHidden: { type: boolean }
        editedAt: { type: integer, nullable: true }
        createdAt: { type: integer }
        quotedCommentId: { type: string, nullable: true }
        isSolved: { type: boolean }
        canEdit: { type: boolean, description: Server-authoritative author edit permission for this viewer. }
        canDelete: { type: boolean, description: Server-authoritative author delete permission for this viewer. }
        canModerate: { type: boolean, description: Server-authoritative moderation permission including role hierarchy. }
    CommentInput:
      type: object
      required: [body]
      properties:
        parentId: { type: string }
        body: { type: string, minLength: 1, maxLength: 16000 }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        attachmentAssetIds:
          type: array
          maxItems: 4
          uniqueItems: true
          description: Ordered asset ids that must exactly match markdown_v1 yourtj-asset image destinations.
          items: { type: string, pattern: '^[1-9][0-9]*$' }
        quotedCommentId: { type: string }
    CommentUpdateInput:
      type: object
      required: [body]
      properties:
        expectedVersion:
          type: integer
          format: int64
          minimum: 1
          default: 1
          description: Compare-and-swap version. Legacy omission is treated as version 1 and conflicts once content has changed.
        body: { type: string, minLength: 1, maxLength: 16000 }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        attachmentAssetIds:
          type: array
          maxItems: 4
          uniqueItems: true
          description: Ordered asset ids that must exactly match markdown_v1 yourtj-asset image destinations.
          items: { type: string, pattern: '^[1-9][0-9]*$' }
    VoteInput:
      type: object
      required: [value, postType]
      properties:
        value: { type: string, enum: [up, down] }
        postType: { type: string, enum: [thread, comment] }
    VoteResponse:
      type: object
      required: [ok, voteCount, viewerVote]
      properties:
        ok: { type: boolean }
        voteCount: { type: integer }
        viewerVote: { type: string, enum: [up, down], nullable: true }
    FlagInput:
      type: object
      required: [reason, postType]
      properties:
        reason: { type: string, enum: [spam, abuse, off_topic, illegal, other] }
        note: { type: string, maxLength: 1000 }
        postType: { type: string, enum: [thread, comment] }
    FlagResponse:
      type: object
      required: [ok, autoHidden, autoSilenced]
      properties:
        ok: { type: boolean }
        autoHidden: { type: boolean }
        autoSilenced: { type: boolean }
    FlagResolveInput:
      type: object
      required: [action, note]
      properties:
        action: { type: string, enum: [uphold, reject, ignore] }
        note: { type: string, minLength: 3, maxLength: 500 }
    Subscription:
      type: object
      required: [targetType, targetId, level, createdAt]
      properties:
        targetType: { type: string, enum: [board, thread] }
        targetId: { type: string }
        level: { type: string, enum: [watching, tracking, muted] }
        createdAt: { type: integer }
    SubscriptionPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Subscription" } } } }
    SubscriptionInput: { type: object, required: [targetType, targetId, level], properties: { targetType: { type: string, enum: [board, thread] }, targetId: { type: string }, level: { type: string, enum: [watching, tracking, muted] } } }
    UnsubscribeInput: { type: object, required: [targetType, targetId], properties: { targetType: { type: string, enum: [board, thread] }, targetId: { type: string } } }
    Bookmark:
      type: object
      required: [targetType, targetId, note, createdAt, content]
      properties:
        targetType: { type: string, enum: [thread, comment] }
        targetId: { type: string }
        note: { type: string, nullable: true }
        createdAt: { type: integer, description: Unix timestamp when the current account bookmarked the content. }
        content: { $ref: "#/components/schemas/ProfileContent" }
    BookmarkInput:
      type: object
      required: [postType]
      properties:
        postType: { type: string, enum: [thread, comment] }
        note: { type: string, maxLength: 500 }
    ModAction: { type: object, properties: { id: { type: string }, actorId: { type: string }, action: { type: string }, targetType: { type: string }, targetId: { type: string }, reason: { type: string, nullable: true }, createdAt: { type: integer } } }
    Notification:
      type: object
      required: [id, type, payload, targetUrl, read, readAt, createdAt]
      properties:
        id: { type: string }
        type: { type: string }
        payload: { type: object, additionalProperties: true }
        targetUrl:
          type: string
          nullable: true
          description: Safe application-relative destination when this notification has one.
        read: { type: boolean }
        readAt: { type: integer, nullable: true }
        createdAt: { type: integer }
    NotificationReadInput:
      type: object
      properties:
        ids:
          type: array
          minItems: 1
          maxItems: 100
          items: { type: string }
          description: Mark only these notifications. They must belong to the current account.
        all:
          type: boolean
          description: Mark every notification for the current account. Cannot be combined with ids.
    InAppNotificationPrefs:
      type: object
      additionalProperties: false
      required: [replies, mentions, quotes, votes, badges, follows, subscriptions, directMessages]
      properties:
        replies: { type: boolean, default: true }
        mentions: { type: boolean, default: true }
        quotes: { type: boolean, default: true }
        votes: { type: boolean, default: true }
        badges: { type: boolean, default: true }
        follows: { type: boolean, default: true }
        subscriptions: { type: boolean, default: true }
        directMessages: { type: boolean, default: true }
    EmailNotificationPrefs:
      type: object
      additionalProperties: false
      required: [weeklyDigest]
      properties:
        weeklyDigest: { type: boolean, default: false }
    NotificationPreferences:
      type: object
      additionalProperties: false
      required: [inApp, email]
      properties:
        inApp: { $ref: "#/components/schemas/InAppNotificationPrefs" }
        email: { $ref: "#/components/schemas/EmailNotificationPrefs" }
    InAppNotificationPrefsInput:
      type: object
      additionalProperties: false
      required: [replies, mentions, quotes, votes, badges, subscriptions, directMessages]
      properties:
        replies: { type: boolean }
        mentions: { type: boolean }
        quotes: { type: boolean }
        votes: { type: boolean }
        badges: { type: boolean }
        follows:
          type: boolean
          description: Optional only during rolling deployment; omission preserves the stored value.
        subscriptions: { type: boolean }
        directMessages: { type: boolean }
    NotificationPreferencesInput:
      type: object
      additionalProperties: false
      required: [inApp, email]
      properties:
        inApp: { $ref: "#/components/schemas/InAppNotificationPrefsInput" }
        email: { $ref: "#/components/schemas/EmailNotificationPrefs" }
    NotificationPrefs:
      type: object
      additionalProperties: false
      required: [prefs]
      properties: { prefs: { $ref: "#/components/schemas/NotificationPreferences" } }
    NotificationPrefsInput:
      type: object
      additionalProperties: false
      required: [prefs]
      properties: { prefs: { $ref: "#/components/schemas/NotificationPreferencesInput" } }
    ReadTrackingInput:
      type: object
      properties:
        lastReadCommentId:
          type: string
          nullable: true
          description: When null or omitted, mark through the thread's current last visible comment.
    Sanction: { type: object, properties: { id: { type: string }, accountId: { type: string }, kind: { type: string, enum: [silence, suspend] }, reason: { type: string }, issuedBy: { type: string, nullable: true }, startsAt: { type: integer }, endsAt: { type: integer, nullable: true }, revokedAt: { type: integer, nullable: true }, createdAt: { type: integer } } }
    SanctionInput: { type: object, required: [reason], properties: { reason: { type: string, minLength: 3, maxLength: 500 }, endsAt: { type: integer, nullable: true } } }
    UnsanctionInput:
      type: object
      required: [sanctionId, reason]
      properties:
        sanctionId: { type: string }
        reason: { type: string, minLength: 3, maxLength: 500 }
    WatchedWord: { type: object, properties: { id: { type: string }, word: { type: string }, action: { type: string, enum: [block, censor, queue] }, createdAt: { type: integer } } }
    AdminBoardCreateInput:
      type: object
      required: [slug, name, reason]
      properties:
        slug: { type: string, minLength: 1, maxLength: 64 }
        name: { type: string, minLength: 1, maxLength: 100 }
        description: { type: string, maxLength: 500 }
        position: { type: integer, minimum: 0, default: 0 }
        isLocked: { type: boolean, default: false }
        minTrustToPost: { type: integer, minimum: 1, maximum: 6, default: 1 }
        isQa: { type: boolean, default: false }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AdminBoardUpdateInput:
      type: object
      required: [reason]
      properties:
        slug: { type: string, minLength: 1, maxLength: 64 }
        name: { type: string, minLength: 1, maxLength: 100 }
        description: { type: string, maxLength: 500 }
        position: { type: integer, minimum: 0 }
        isLocked: { type: boolean }
        minTrustToPost: { type: integer, minimum: 1, maximum: 6 }
        isQa: { type: boolean }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AdminTagCreateInput:
      type: object
      required: [slug, name, reason]
      properties:
        slug: { type: string, minLength: 1, maxLength: 64 }
        name: { type: string, minLength: 1, maxLength: 100 }
        description: { type: string, maxLength: 500 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AdminTagUpdateInput:
      type: object
      required: [reason]
      properties:
        slug: { type: string, minLength: 1, maxLength: 64 }
        name: { type: string, minLength: 1, maxLength: 100 }
        description: { type: string, maxLength: 500 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    WatchedWordInput:
      type: object
      required: [word, action, reason]
      properties:
        word: { type: string, minLength: 1, maxLength: 200 }
        action: { type: string, enum: [block, censor, queue] }
        reason: { type: string, minLength: 3, maxLength: 500 }

    # ---- platform / admin ----
    Setting: { type: object, properties: { key: { type: string }, value: { type: string } } }
    SettingUpdateInput:
      type: object
      required: [value, reason]
      properties:
        value: { type: string, maxLength: 20000 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AnnouncementReceipt:
      type: object
      required: [revision]
      properties:
        revision: { type: integer, minimum: 1 }
        firstSeenAt: { type: integer, nullable: true }
        dismissedAt: { type: integer, nullable: true }
        acknowledgedAt: { type: integer, nullable: true }
    AnnouncementReceiptSummary:
      type: object
      required: [seenCount, dismissedCount, acknowledgedCount]
      properties:
        seenCount: { type: integer, minimum: 0 }
        dismissedCount: { type: integer, minimum: 0 }
        acknowledgedCount: { type: integer, minimum: 0 }
    Announcement:
      type: object
      required: [id, title, status, effectiveState, presentation, severity, priority, audience, requiresAck, version, revision, createdAt, updatedAt]
      properties:
        id: { type: string }
        title: { type: string }
        body: { type: string, nullable: true }
        status: { type: string, enum: [draft, scheduled, published, archived] }
        effectiveState: { type: string, enum: [draft, scheduled, active, expired, archived] }
        presentation: { type: string, enum: [card, banner] }
        severity: { type: string, enum: [info, success, warning, critical] }
        priority: { type: integer, minimum: -1000, maximum: 1000 }
        audience: { type: string, enum: [all, authenticated, staff] }
        requiresAck: { type: boolean }
        version: { type: integer, minimum: 1 }
        revision: { type: integer, minimum: 1 }
        startsAt: { type: integer, nullable: true }
        endsAt: { type: integer, nullable: true }
        publishedAt: { type: integer, nullable: true }
        archivedAt: { type: integer, nullable: true }
        createdAt: { type: integer }
        updatedAt: { type: integer }
        receipt:
          allOf: [{ $ref: "#/components/schemas/AnnouncementReceipt" }]
          nullable: true
        receiptSummary:
          allOf: [{ $ref: "#/components/schemas/AnnouncementReceiptSummary" }]
          nullable: true
    AnnouncementCreateInput:
      type: object
      required: [title, status, presentation, severity, priority, audience, requiresAck, reason]
      properties:
        title: { type: string, minLength: 1, maxLength: 200 }
        body: { type: string, nullable: true, maxLength: 20000 }
        status: { type: string, enum: [draft, scheduled, published] }
        presentation: { type: string, enum: [card, banner] }
        severity: { type: string, enum: [info, success, warning, critical] }
        priority: { type: integer, minimum: -1000, maximum: 1000 }
        audience: { type: string, enum: [all, authenticated, staff] }
        requiresAck: { type: boolean }
        startsAt: { type: integer, nullable: true }
        endsAt: { type: integer, nullable: true }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AnnouncementUpdateInput:
      allOf:
        - $ref: "#/components/schemas/AnnouncementCreateInput"
        - type: object
          required: [expectedVersion, bumpRevision]
          properties:
            expectedVersion: { type: integer, minimum: 1 }
            bumpRevision:
              type: boolean
              description: Re-present this update to users who already saw the current revision.
    AdminVersionedArchiveInput:
      type: object
      required: [expectedVersion, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AnnouncementReceiptInput:
      type: object
      required: [revision, action]
      properties:
        revision: { type: integer, minimum: 1 }
        action: { type: string, enum: [seen, dismiss, acknowledge] }
    AnnouncementRevision:
      type: object
      required: [announcementId, version, revision, title, status, presentation, severity, priority, audience, requiresAck, createdAt]
      properties:
        announcementId: { type: string }
        version: { type: integer, minimum: 1 }
        revision: { type: integer, minimum: 1 }
        title: { type: string }
        body: { type: string, nullable: true }
        status: { type: string, enum: [draft, scheduled, published, archived] }
        presentation: { type: string, enum: [card, banner] }
        severity: { type: string, enum: [info, success, warning, critical] }
        priority: { type: integer }
        audience: { type: string, enum: [all, authenticated, staff] }
        requiresAck: { type: boolean }
        startsAt: { type: integer, nullable: true }
        endsAt: { type: integer, nullable: true }
        createdAt: { type: integer }
    Promotion:
      type: object
      required: [id, placement, title, targetUrl, assetDelivery, status, effectiveState, priority, audience, version, createdAt, updatedAt, trackingToken, metrics]
      properties:
        id: { type: string }
        placement: { type: string, enum: [home-left-primary, home-left-secondary] }
        title: { type: string }
        body: { type: string, nullable: true }
        ctaLabel: { type: string, nullable: true }
        targetUrl: { type: string, description: Same-origin relative application path. }
        assetId: { type: string, nullable: true }
        assetDelivery:
          allOf: [{ $ref: "#/components/schemas/MediaDelivery" }]
          nullable: true
          description: Current short-lived sanitized CDN projection; null when no asset is configured or publication is unavailable.
        status: { type: string, enum: [draft, scheduled, published, paused, archived] }
        effectiveState: { type: string, enum: [draft, scheduled, active, paused, expired, archived] }
        priority: { type: integer, minimum: -1000, maximum: 1000 }
        audience: { type: string, enum: [all, authenticated, staff] }
        version: { type: integer, minimum: 1 }
        startsAt: { type: integer, nullable: true }
        endsAt: { type: integer, nullable: true }
        archivedAt: { type: integer, nullable: true }
        createdAt: { type: integer }
        updatedAt: { type: integer }
        trackingToken:
          type: string
          nullable: true
          description: Short-lived anonymous presentation token returned only by the active public list.
        metrics:
          allOf: [{ $ref: "#/components/schemas/PromotionMetricSummary" }]
          nullable: true
          description: Rolling 30-day aggregate returned only by the administration list.
    PromotionMetricSummary:
      type: object
      additionalProperties: false
      required: [from, to, impressions, clicks]
      properties:
        from: { type: string, format: date }
        to: { type: string, format: date }
        impressions: { type: integer, minimum: 0 }
        clicks: { type: integer, minimum: 0 }
    PromotionMetricDay:
      type: object
      additionalProperties: false
      required: [metricDate, impressions, clicks]
      properties:
        metricDate: { type: string, format: date }
        impressions: { type: integer, minimum: 0 }
        clicks: { type: integer, minimum: 0 }
    PromotionMetrics:
      type: object
      additionalProperties: false
      required: [summary, days]
      properties:
        summary: { $ref: "#/components/schemas/PromotionMetricSummary" }
        days:
          type: array
          items: { $ref: "#/components/schemas/PromotionMetricDay" }
    PromotionEventInput:
      type: object
      additionalProperties: false
      required: [eventType, trackingToken]
      properties:
        eventType: { type: string, enum: [impression, click] }
        trackingToken: { type: string, minLength: 1, maxLength: 2048 }
    PromotionCreateInput:
      type: object
      required: [placement, title, targetUrl, status, priority, audience, reason]
      properties:
        placement: { type: string, enum: [home-left-primary, home-left-secondary] }
        title: { type: string, minLength: 1, maxLength: 120 }
        body: { type: string, nullable: true, maxLength: 500 }
        ctaLabel: { type: string, nullable: true, maxLength: 40 }
        targetUrl: { type: string, minLength: 1, maxLength: 2048, description: Same-origin relative application path. }
        assetId: { type: string, nullable: true }
        status: { type: string, enum: [draft, scheduled, published, paused] }
        priority: { type: integer, minimum: -1000, maximum: 1000 }
        audience: { type: string, enum: [all, authenticated, staff] }
        startsAt: { type: integer, nullable: true }
        endsAt: { type: integer, nullable: true }
        reason: { type: string, minLength: 3, maxLength: 500 }
    PromotionUpdateInput:
      allOf:
        - $ref: "#/components/schemas/PromotionCreateInput"
        - type: object
          required: [expectedVersion]
          properties:
            expectedVersion: { type: integer, minimum: 1 }
    AdminCourseCreateInput:
      type: object
      required: [code, name, reason]
      properties:
        code: { type: string, minLength: 1, maxLength: 64 }
        name: { type: string, minLength: 1, maxLength: 200 }
        credit: { type: number, minimum: 0, maximum: 100 }
        department: { type: string, maxLength: 200 }
        teacherName: { type: string, maxLength: 200 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AdminCourseUpdateInput:
      type: object
      required: [reason]
      properties:
        code: { type: string, minLength: 1, maxLength: 64 }
        name: { type: string, minLength: 1, maxLength: 200 }
        credit: { type: number, minimum: 0, maximum: 100 }
        department: { type: string, maxLength: 200 }
        teacherName: { type: string, maxLength: 200 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    # ---- typed pages ----
    CoursePage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Course" } } } }
    ReviewPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Review" } } } }
    ThreadPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Thread" } } } }
    CommentPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Comment" } } } }
    NotificationPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Notification" } } } }
    NotificationUnreadCount:
      type: object
      required: [count]
      properties:
        count: { type: integer, format: int64, minimum: 0 }
    AppealPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Appeal" } } } }
    AdminAppealPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AdminAppeal" } } } }
    GovernanceNoticePage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/GovernanceNotice" } } } }
    TaskPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Task" } } } }
    ProductPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Product" } } } }
    PurchasePage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Purchase" } } } }
    LedgerPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/LedgerEntry" } } } }
    CreditReconciliationRunPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/CreditReconciliationRun" } } } }
    CreditReconciliationWalletPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/CreditReconciliationWallet" } } } }
    ReportPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Report" } } } }
    ThreadFeedPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/ThreadFeed" } } } }
    UserThreadPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/UserThread" } } } }
    UserCommentPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/UserComment" } } } }
    ProfileContentPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/ProfileContent" } } } }
    BookmarkPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Bookmark" } } } }
    UserSummaryPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/UserSummary" } } } }
    RevisionPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/PostRevision" } } } }
    ModActionPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/ModAction" } } } }

    # ---- forum: DM ----
    DmConversation:
      type: object
      required: [id, participantId, participantHandle, unreadCount, isArchived, isMuted, isDeleted, requestStatus, canSend, createdAt]
      properties:
        id: { type: string }
        participantId: { type: string }
        participantHandle: { type: string }
        participantDisplayName: { type: string, nullable: true }
        participantAvatarUrl: { type: string, nullable: true, description: Short-lived clean thumb_256 compatibility URL; refresh the owning conversation response after expiry. }
        lastMessageExcerpt: { type: string, nullable: true }
        lastMessageAt: { type: integer, nullable: true }
        unreadCount: { type: integer, minimum: 0 }
        isArchived: { type: boolean }
        isMuted: { type: boolean }
        isDeleted: { type: boolean }
        requestStatus: { type: string, enum: [accepted, pending] }
        requestDirection: { type: string, enum: [incoming, outgoing], nullable: true }
        canSend: { type: boolean, description: False while a one-message request awaits acceptance. }
        createdAt: { type: integer }
    DmConversationInput:
      type: object
      required: [recipientHandle]
      properties:
        recipientHandle: { type: string, minLength: 3, maxLength: 30 }
        requestMessage:
          type: string
          minLength: 1
          maxLength: 1000
          description: Required when the recipient does not already accept direct delivery from the sender; it is the only message allowed before acceptance.
    DmCounts:
      type: object
      required: [count, unreadCount, requestCount]
      properties:
        count: { type: integer, minimum: 0, description: unreadCount plus requestCount for compatibility badges. }
        unreadCount: { type: integer, minimum: 0 }
        requestCount: { type: integer, minimum: 0 }
    DmMessage:
      type: object
      required: [id, conversationId, senderId, senderHandle, body, createdAt]
      properties:
        id: { type: string }
        conversationId: { type: string }
        senderId: { type: string }
        senderHandle: { type: string }
        senderDisplayName: { type: string, nullable: true }
        body: { type: string }
        createdAt: { type: integer }
    DmMessageInput:
      type: object
      required: [body]
      properties:
        body: { type: string, minLength: 1, maxLength: 16000 }
        clientMessageId:
          type: string
          format: uuid
          description: Optional client-generated idempotency identity; retries with the same value must keep the same conversation and body.
    DmReadInput:
      type: object
      properties:
        lastReadMessageId: { type: string, nullable: true }
    DmReportInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, enum: [spam, abuse, harassment, fraud, illegal, other] }
        note: { type: string, maxLength: 1000 }
    DmReport:
      type: object
      required: [id, messageId, conversationId, reporterId, senderId, reason, status, createdAt]
      properties:
        id: { type: string }
        messageId: { type: string }
        conversationId: { type: string }
        reporterId: { type: string }
        reporterHandle: { type: string }
        reporterDisplayName: { type: string, nullable: true }
        senderId: { type: string }
        senderHandle: { type: string }
        senderDisplayName: { type: string, nullable: true }
        messageExcerpt: { type: string }
        reason: { type: string, enum: [spam, abuse, harassment, fraud, illegal, other] }
        note: { type: string, nullable: true }
        status: { type: string, enum: [open, upheld, rejected] }
        handledBy: { type: string, nullable: true }
        handledAt: { type: integer, nullable: true }
        createdAt: { type: integer }
    DmReportResolutionInput:
      type: object
      required: [action]
      properties:
        action: { type: string, enum: [uphold, reject] }
        note: { type: string, maxLength: 1000 }
    # ---- forum: polls ----
    PollInput:
      type: object
      required: [question, options]
      properties:
        question: { type: string, minLength: 1, maxLength: 500 }
        multiSelect: { type: boolean, default: false }
        closesAt: { type: integer, nullable: true }
        options:
          type: array
          minItems: 2
          maxItems: 20
          uniqueItems: true
          items: { type: string, minLength: 1, maxLength: 200 }
    PollOption:
      type: object
      required: [id, label, voteCount, position]
      properties:
        id: { type: string }
        label: { type: string }
        voteCount: { type: integer, minimum: 0 }
        position: { type: integer, minimum: 0 }
    Poll:
      type: object
      required: [id, question, multiSelect, closesAt, options, myVotes]
      properties:
        id: { type: string }
        question: { type: string }
        multiSelect: { type: boolean }
        closesAt: { type: integer, nullable: true }
        options:
          type: array
          items: { $ref: "#/components/schemas/PollOption" }
        myVotes: { type: array, items: { type: string } }
    PollVoteResponse:
      type: object
      required: [ok, myVotes]
      properties:
        ok: { type: boolean }
        myVotes: { type: array, items: { type: string } }
    # ---- forum: cross-device drafts ----
    ThreadDraftPayload:
      type: object
      additionalProperties: false
      required: [kind, boardId, title, body, contentFormat, tags, pollQuestion, pollOptions, attachmentAssetIds]
      properties:
        kind: { type: string, enum: [thread] }
        boardId: { type: string, nullable: true }
        title: { type: string, maxLength: 120 }
        body: { type: string, maxLength: 50000 }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        tags:
          type: array
          maxItems: 3
          items: { type: string, minLength: 1, maxLength: 32 }
        pollQuestion: { type: string, maxLength: 300 }
        pollOptions:
          type: array
          maxItems: 20
          items: { type: string, maxLength: 200 }
        attachmentAssetIds:
          type: array
          maxItems: 8
          uniqueItems: true
          description: Owner-only upload ids retained across devices. Pending/blocked state is not public binding authorization.
          items: { type: string, pattern: '^[1-9][0-9]*$' }
    CommentDraftPayload:
      type: object
      additionalProperties: false
      required: [kind, threadId, body, contentFormat, parentId, attachmentAssetIds]
      properties:
        kind: { type: string, enum: [comment] }
        threadId: { type: string }
        body: { type: string, maxLength: 16000 }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        parentId: { type: string, nullable: true }
        attachmentAssetIds:
          type: array
          maxItems: 4
          uniqueItems: true
          description: Owner-only upload ids retained across devices. Pending/blocked state is not public binding authorization.
          items: { type: string, pattern: '^[1-9][0-9]*$' }
    ForumDraftPayload:
      oneOf:
        - { $ref: "#/components/schemas/ThreadDraftPayload" }
        - { $ref: "#/components/schemas/CommentDraftPayload" }
      discriminator:
        propertyName: kind
        mapping:
          thread: "#/components/schemas/ThreadDraftPayload"
          comment: "#/components/schemas/CommentDraftPayload"
    DraftOutput:
      type: object
      required: [draftKey, payload, version, updatedAt]
      properties:
        draftKey: { type: string }
        payload: { $ref: "#/components/schemas/ForumDraftPayload" }
        version: { type: integer, minimum: 1 }
        updatedAt: { type: integer, format: int64 }
    DraftSaveInput:
      type: object
      additionalProperties: false
      required: [draftKey, expectedVersion, payload]
      properties:
        draftKey: { type: string }
        expectedVersion:
          type: integer
          minimum: 0
          description: Zero creates a new draft; a positive value compare-and-swaps that version.
        payload: { $ref: "#/components/schemas/ForumDraftPayload" }
    # ---- social identity ----
    IgnoreUser:
      type: object
      deprecated: true
      properties:
        accountId: { type: string }
        handle: { type: string }
        avatarUrl: { type: string, nullable: true, description: Short-lived clean thumb_256 compatibility URL; refresh the owning ignore-list response after expiry. }
        createdAt: { type: integer }
    UserSummary:
      type: object
      required: [id, handle, displayName, avatarUrl, role, followedAt]
      properties:
        id: { type: string }
        handle: { type: string }
        displayName: { type: string, nullable: true }
        avatarUrl: { type: string, nullable: true, description: Short-lived clean thumb_256 compatibility URL; refresh the owning relationship-list response after expiry. }
        role: { type: string, enum: [user, mod, admin] }
        followedAt: { type: integer }
    UserRelationship:
      type: object
      required: [isSelf, following, followedBy, muted, blockedByMe, blockedMe, canFollow, canStartConversation, canMention]
      properties:
        isSelf: { type: boolean }
        following: { type: boolean }
        followedBy: { type: boolean }
        muted: { type: boolean }
        blockedByMe: { type: boolean }
        blockedMe: { type: boolean }
        canFollow: { type: boolean }
        canStartConversation:
          type: boolean
          description: Whether the current account may start a new conversation under block and recipient DM policy.
        canMention:
          type: boolean
          description: Whether the current account may create a semantic mention notification under block and recipient mention policy.
    # ---- public profile ----
    UserBadge:
      type: object
      required: [slug, name]
      properties:
        slug: { type: string }
        name: { type: string }
    VerificationCategory:
      type: string
      enum: [identity, special]
    VerificationIcon:
      type: string
      description: Controlled icon token rendered with the first-party icon library.
      enum: [badge-check, building-2, shield-check, sparkles]
    VerificationBadgeVariant:
      type: string
      description: Controlled semantic badge variant; arbitrary CSS and colors are not accepted.
      enum: [default, secondary, outline]
    PublicVerification:
      type: object
      required: [slug, category, label, icon, badgeVariant, issuedAt]
      properties:
        slug: { type: string }
        category: { $ref: "#/components/schemas/VerificationCategory" }
        label: { type: string }
        description: { type: string, nullable: true }
        icon: { $ref: "#/components/schemas/VerificationIcon" }
        badgeVariant: { $ref: "#/components/schemas/VerificationBadgeVariant" }
        issuedAt: { type: integer }
        expiresAt: { type: integer, nullable: true }
    UserProfile:
      type: object
      required: [id, handle, displayName, school, bio, website, avatarUrl, bannerUrl, role, trustLevel, badges, verifications, threadCount, commentCount, votesReceived, followerCount, followingCount, canViewActivity, createdAt]
      properties:
        id: { type: string }
        handle: { type: string }
        displayName: { type: string, nullable: true }
        school: { type: string, minLength: 1, maxLength: 100, description: Owner-editable public school label governed by profile visibility. }
        bio: { type: string, nullable: true }
        website: { type: string, format: uri, nullable: true }
        avatarUrl: { type: string, nullable: true, description: Short-lived clean thumb_256 compatibility URL; refresh the owning profile response after expiry. }
        bannerUrl: { type: string, nullable: true }
        role: { type: string, enum: [user, mod, admin] }
        trustLevel: { type: integer, minimum: 0, maximum: 6 }
        badges: { type: array, items: { $ref: "#/components/schemas/UserBadge" } }
        verifications:
          type: array
          description: Active grants explicitly allowed for public profile display; never contains evidence, issuer, or staff reason.
          items: { $ref: "#/components/schemas/PublicVerification" }
        threadCount: { type: integer, minimum: 0 }
        commentCount: { type: integer, minimum: 0 }
        votesReceived: { type: integer, minimum: 0 }
        followerCount: { type: integer, minimum: 0 }
        followingCount: { type: integer, minimum: 0 }
        canViewActivity:
          type: boolean
          description: Viewer-specific permission for authored-content lists and future activity, media, and likes tabs. Aggregate public-content counters remain visible with the profile.
        createdAt: { type: integer }
    UserThread:
      type: object
      required: [id, title, bodyExcerpt, contentFormat, boardSlug, replyCount, voteCount, viewerVote, isBookmarked, attachments, createdAt]
      properties:
        id: { type: string }
        title: { type: string }
        bodyExcerpt: { type: string, nullable: true }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        boardSlug: { type: string }
        replyCount: { type: integer, minimum: 0 }
        voteCount: { type: integer }
        viewerVote: { type: string, enum: [up, down], nullable: true }
        isBookmarked: { type: boolean }
        attachments: { type: array, maxItems: 1, items: { $ref: "#/components/schemas/ForumAttachment" } }
        createdAt: { type: integer }
    UserComment:
      type: object
      required: [id, threadId, threadTitle, body, contentFormat, replyCount, voteCount, viewerVote, isBookmarked, attachments, createdAt]
      properties:
        id: { type: string }
        threadId: { type: string }
        threadTitle: { type: string }
        body: { type: string }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        replyCount: { type: integer, minimum: 0, description: Count of directly nested comments that remain visible. }
        voteCount: { type: integer }
        viewerVote: { type: string, enum: [up, down], nullable: true }
        isBookmarked: { type: boolean }
        attachments: { type: array, maxItems: 4, items: { $ref: "#/components/schemas/ForumAttachment" } }
        createdAt: { type: integer }
    ProfileContent:
      type: object
      description: One currently visible Forum thread or comment projected for profile media, likes, or owner bookmarks.
      required: [targetType, id, threadId, title, body, contentFormat, boardSlug, authorHandle, authorDisplayName, replyCount, voteCount, viewerVote, isBookmarked, attachments, createdAt, activityAt]
      properties:
        targetType: { type: string, enum: [thread, comment] }
        id: { type: string }
        threadId: { type: string }
        title: { type: string }
        body: { type: string, nullable: true }
        contentFormat: { $ref: "#/components/schemas/ContentFormat" }
        boardSlug: { type: string }
        authorHandle: { type: string }
        authorDisplayName: { type: string, nullable: true }
        replyCount: { type: integer, minimum: 0 }
        voteCount: { type: integer }
        viewerVote: { type: string, enum: [up, down], nullable: true }
        isBookmarked: { type: boolean }
        attachments: { type: array, maxItems: 8, items: { $ref: "#/components/schemas/ForumAttachment" } }
        createdAt: { type: integer, description: Canonical content creation time. }
        activityAt: { type: integer, description: Like time for liked content; otherwise the content creation time. }
    UserProfileWithStats:
      allOf: [{ $ref: "#/components/schemas/UserProfile" }]
      deprecated: true
    # ---- activity ----
    ActivityWeights:
      type: object
      required: [thread, comment, like, checkIn]
      properties:
        thread: { type: integer, minimum: 0, maximum: 1000 }
        comment: { type: integer, minimum: 0, maximum: 1000 }
        like: { type: integer, minimum: 0, maximum: 1000 }
        checkIn: { type: integer, minimum: 0, maximum: 1000 }
    ActivityDay:
      type: object
      required: [date, threads, comments, likes, checkIns, score]
      properties:
        date: { type: string, format: date }
        threads: { type: integer, minimum: 0 }
        comments: { type: integer, minimum: 0 }
        likes: { type: integer, minimum: 0, description: Positive likes given by the user. }
        checkIns: { type: integer, minimum: 0, maximum: 1, description: Idempotent daily check-in recorded on the server's Asia/Shanghai calendar. }
        score: { type: integer, minimum: 0 }
    ActivityCalendar:
      type: object
      description: A continuous Asia/Shanghai calendar; days without activity are returned with zero counts.
      required: [timezone, from, to, policyVersion, trustPolicyVersion, weights, likeDailyCap, days]
      properties:
        timezone: { type: string, enum: [Asia/Shanghai] }
        from: { type: string, format: date }
        to: { type: string, format: date }
        policyVersion: { type: integer, minimum: 1, description: Activity weight policy version. }
        trustPolicyVersion: { type: integer, minimum: 1, description: Trust policy version supplying the daily like cap. }
        weights: { $ref: "#/components/schemas/ActivityWeights" }
        likeDailyCap: { type: integer, minimum: 0, description: Maximum daily score contributed by positive likes. }
        days: { type: array, items: { $ref: "#/components/schemas/ActivityDay" } }
    ActivityPolicy:
      type: object
      required: [version, timezone, weights, reason, changedBy, createdAt]
      properties:
        version: { type: integer, minimum: 1 }
        timezone: { type: string, enum: [Asia/Shanghai] }
        weights: { $ref: "#/components/schemas/ActivityWeights" }
        reason: { type: string }
        changedBy: { type: string }
        createdAt: { type: integer }
    ActivityPolicyUpdateInput:
      type: object
      required: [expectedVersion, weights, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        weights: { $ref: "#/components/schemas/ActivityWeights" }
        reason: { type: string, minLength: 3, maxLength: 500 }
    CheckInStatus:
      type: object
      description: Current account's daily check-in state on the canonical Asia/Shanghai calendar.
      required: [timezone, date, checkedIn, newlyCheckedIn, checkedInAt, currentStreak, totalDays, nextResetAt]
      properties:
        timezone: { type: string, enum: [Asia/Shanghai] }
        date: { type: string, format: date }
        checkedIn: { type: boolean }
        newlyCheckedIn: { type: boolean, description: True only when this request created today's check-in. Always false on GET. }
        checkedInAt: { type: integer, nullable: true, description: "Unix seconds for today's check-in, or null before check-in." }
        currentStreak: { type: integer, minimum: 0, description: "Consecutive checked days ending today, or yesterday before today's check-in." }
        totalDays: { type: integer, minimum: 0 }
        nextResetAt: { type: integer, description: Unix seconds for the next Asia/Shanghai midnight. }
    TrustProgress:
      type: object
      required: [trustLevel, teaName, qualifyingScore, nextLevel, nextThreshold, remainingScore, progressPercent, policyVersion, isMaxLevel, overrideActive, promotionBlockedUntil, promotionRequiresNewActivity]
      properties:
        trustLevel: { type: integer, minimum: 1, maximum: 6 }
        teaName: { type: string }
        qualifyingScore: { type: integer, minimum: 0 }
        nextLevel: { type: integer, minimum: 2, maximum: 6, nullable: true }
        nextThreshold: { type: integer, minimum: 1, nullable: true }
        remainingScore: { type: integer, minimum: 0, nullable: true }
        progressPercent: { type: integer, minimum: 0, maximum: 100 }
        policyVersion: { type: integer, minimum: 1 }
        isMaxLevel: { type: boolean }
        overrideActive: { type: boolean }
        promotionBlockedUntil: { type: integer, nullable: true }
        promotionRequiresNewActivity: { type: boolean }
    TrustLevelPolicy:
      type: object
      required: [version, scorePolicyVersion, thresholdLevel2, thresholdLevel3, thresholdLevel4, thresholdLevel5, thresholdLevel6, likeDailyCap, demotionCooldownDays, reason, changedBy, createdAt]
      properties:
        version: { type: integer, minimum: 1 }
        scorePolicyVersion: { type: integer, minimum: 1 }
        thresholdLevel2: { type: integer, minimum: 1 }
        thresholdLevel3: { type: integer, minimum: 1 }
        thresholdLevel4: { type: integer, minimum: 1 }
        thresholdLevel5: { type: integer, minimum: 1 }
        thresholdLevel6: { type: integer, minimum: 1 }
        likeDailyCap: { type: integer, minimum: 0, maximum: 100000 }
        demotionCooldownDays: { type: integer, minimum: 0, maximum: 365 }
        reason: { type: string }
        changedBy: { type: string }
        createdAt: { type: integer }
    TrustLevelPolicyUpdateInput:
      type: object
      required: [expectedVersion, thresholdLevel2, thresholdLevel3, thresholdLevel4, thresholdLevel5, thresholdLevel6, likeDailyCap, demotionCooldownDays, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        thresholdLevel2: { type: integer, minimum: 1 }
        thresholdLevel3: { type: integer, minimum: 1 }
        thresholdLevel4: { type: integer, minimum: 1 }
        thresholdLevel5: { type: integer, minimum: 1 }
        thresholdLevel6: { type: integer, minimum: 1 }
        likeDailyCap: { type: integer, minimum: 0, maximum: 100000 }
        demotionCooldownDays: { type: integer, minimum: 0, maximum: 365 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    TrustLevelAdjustInput:
      type: object
      required: [reason]
      properties:
        trustLevel: { type: integer, minimum: 1, maximum: 6, nullable: true }
        clearOverride: { type: boolean, default: false }
        reason: { type: string, minLength: 3, maxLength: 500 }
    TrustLevelEvent:
      type: object
      required: [id, accountId, eventKind, fromLevel, toLevel, qualifyingScore, policyVersion, actorKind, actorAccountId, reason, governanceEventId, createdAt]
      properties:
        id: { type: string }
        accountId: { type: string }
        eventKind: { type: string, enum: [upgrade, demotion, manual_set, override_clear, backfill_initialized, registration] }
        fromLevel: { type: integer, minimum: 0, maximum: 6 }
        toLevel: { type: integer, minimum: 1, maximum: 6 }
        qualifyingScore: { type: integer, minimum: 0 }
        policyVersion: { type: integer, minimum: 1 }
        actorKind: { type: string, enum: [system, account] }
        actorAccountId: { type: string, nullable: true }
        reason: { type: string, nullable: true }
        governanceEventId: { type: string, nullable: true }
        createdAt: { type: integer }
    TrustLevelPolicyPage:
      allOf:
        - { $ref: "#/components/schemas/Page" }
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/TrustLevelPolicy" } } } }
    TrustLevelEventPage:
      allOf:
        - { $ref: "#/components/schemas/Page" }
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/TrustLevelEvent" } } } }
    # ---- platform ----
    OneboxResult:
      type: object
      required: [type, url, title, description, imageUrl, siteName]
      properties:
        type:
          type: string
          enum: [plain, card]
          description: plain means the host is not allowlisted; card means metadata was fetched or read from cache.
        url: { type: string }
        title: { type: string, nullable: true }
        description: { type: string, nullable: true }
        imageUrl:
          type: string
          nullable: true
          description: Reserved for a future platform-proxied image; remote preview images are currently returned as null.
        siteName: { type: string, nullable: true }
    # ---- media ----
    MediaUsage:
      type: string
      description: Optional intended image surface persisted across moderation and page reloads; it is not a business binding.
      enum: [profile_avatar, profile_banner, forum_thread, forum_comment]
    UploadIntentInput:
      type: object
      required: [kind, contentType]
      properties:
        kind: { type: string, enum: [image, file] }
        contentType: { type: string }
        usage:
          allOf: [{ $ref: "#/components/schemas/MediaUsage" }]
          description: Controlled usages require kind=image. Omit for unbound generic uploads.
    UploadCredentials:
      type: object
      required: [uploadIntentId, accessKeyId, accessKeySecret, securityToken, region, bucket, prefix, ossKey, callbackUrl, callbackBody, expiration]
      properties:
        uploadIntentId: { type: string, format: uuid }
        accessKeyId: { type: string }
        accessKeySecret: { type: string }
        securityToken: { type: string }
        region: { type: string }
        bucket: { type: string }
        prefix: { type: string }
        ossKey: { type: string }
        callbackUrl: { type: string, format: uri }
        callbackBody: { type: string }
        expiration: { type: integer, description: Unix seconds }
    MediaDeliveryState:
      type: string
      description: Sanitized Delivery projection state; moderation approval and CDN publication are separate facts.
      enum: [unpublished, processing, published, failed, blocked]
    MediaDeliveryVariant:
      type: string
      description: Server-owned immutable variant profile. Clients cannot request arbitrary dimensions or object paths.
      enum: [thumb_256, display_1280, full_2048]
    Upload:
      type: object
      required: [id, accountId, kind, bytes, mime, status, deliveryState, deliveryErrorCode, usage, imageWidth, imageHeight, isSelfReview, approvalRequirement, deletionState, retentionHeld, retentionState, retentionExpiresAt, createdAt]
      description: Moderation-safe metadata. Storage key, object URL, and content hash are deliberately omitted.
      properties:
        id: { type: string }
        accountId: { type: string }
        kind: { type: string, enum: [image, file] }
        bytes: { type: integer }
        mime: { type: string }
        status: { type: string, enum: [pending, clean, quarantined, blocked] }
        deliveryState: { $ref: "#/components/schemas/MediaDeliveryState" }
        deliveryErrorCode:
          type: string
          nullable: true
          enum: [legacy_animated_format_requires_reupload, lease_expired_after_max_attempts, asset_left_clean_state, invalid_source_length, ingest_read_failed, source_digest_mismatch, image_worker_join_failed, image_decode_rejected, variant_registration_failed, delivery_write_failed, delivery_verification_failed, publication_commit_failed]
          description: Bounded operations-only failure category; it never contains a provider URL, key, response body, or user data.
        usage:
          allOf: [{ $ref: "#/components/schemas/MediaUsage" }]
          nullable: true
        imageWidth: { type: integer, minimum: 1, maximum: 20000, nullable: true }
        imageHeight: { type: integer, minimum: 1, maximum: 20000, nullable: true }
        isSelfReview:
          type: boolean
          description: True only for the current ADMIN's own upload shown under the explicit self-review exception.
        approvalRequirement:
          type: string
          enum: [none, image_preview, scanner, satisfied]
          description: Actor-specific evidence gate. Files remain scanner-gated; image approval requires this moderator's trusted preview.
        deletionState:
          type: string
          enum: [queued, leased, succeeded, dead_letter]
          nullable: true
          description: Durable provider deletion state, present after an upload enters quarantine.
        retentionHeld:
          type: boolean
          description: Whether a purpose-bound, unexpired operations hold currently pauses deletion. The reason and kind are never disclosed here.
        retentionState:
          type: string
          enum: [none, active, expired]
          description: Presence state of the unreleased operations record; expired records must be reviewed in the operations inventory before replacement or release.
        retentionExpiresAt:
          type: integer
          nullable: true
          description: Unix seconds for the unreleased operations record; null only when retentionState is none.
        createdAt: { type: integer }
    MyUpload:
      type: object
      required: [id, kind, usage, bytes, mime, status, deliveryState, imageWidth, imageHeight, createdAt]
      description: Owner-safe upload status; storage keys, hashes, and object URLs are intentionally omitted.
      properties:
        id: { type: string }
        kind: { type: string, enum: [image, file] }
        usage:
          allOf: [{ $ref: "#/components/schemas/MediaUsage" }]
          nullable: true
        bytes: { type: integer, minimum: 0, description: Zero only after an object was physically deleted and its metadata redacted. }
        mime: { type: string }
        status: { type: string, enum: [pending, clean, quarantined, blocked] }
        deliveryState: { $ref: "#/components/schemas/MediaDeliveryState" }
        imageWidth: { type: integer, minimum: 1, maximum: 20000, nullable: true }
        imageHeight: { type: integer, minimum: 1, maximum: 20000, nullable: true }
        createdAt: { type: integer }
    ModerationPreviewGrant:
      type: object
      description: One-time short-lived credential for a same-origin, audited media preview. Contains no provider URL, key, or content hash.
      required: [token, expiresAt]
      properties:
        token: { type: string, minLength: 43, maxLength: 43 }
        expiresAt: { type: integer, description: Unix seconds; grants expire after 60 seconds and are consumed once. }
    MediaModerationInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
        selfReviewConfirmed:
          type: boolean
          default: false
          description: Must be true only when ADMIN intentionally invokes the recent-authenticated own-media exception.
    MediaDelivery:
      type: object
      required: [assetId, variant, url, expiresAt, mime, width, height]
      description: Short-lived CDN capability derived from a currently clean, atomically published asset. It exposes no Ingest bucket/key, Delivery bucket/provider origin/credential, or separate persistent locator; its signed URL may visibly contain an opaque immutable Delivery path.
      properties:
        assetId: { type: string }
        variant: { $ref: "#/components/schemas/MediaDeliveryVariant" }
        url: { type: string, format: uri, description: "Five-minute CDN URL whose path is an opaque immutable Delivery identifier, not an Ingest key or provider URL." }
        expiresAt: { type: integer, description: Unix seconds; clients should refresh before this time. }
        mime: { type: string, enum: [image/webp] }
        width: { type: integer, minimum: 1, maximum: 2048 }
        height: { type: integer, minimum: 1, maximum: 2048 }
    UploadPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Upload" } } } }
    MyUploadPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/MyUpload" } } } }
    AdminForumFlagPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AdminForumFlag" } } } }

    # ---- admin ----
    AdminUser:
      type: object
      required: [id, handle, role, status, trustLevel, createdAt]
      properties:
        id: { type: string }
        handle: { type: string }
        avatarUrl: { type: string, nullable: true }
        role: { type: string, enum: [user, mod, admin] }
        status: { type: string, enum: [active, suspended, deactivated, deletion_requested, deleted, purged] }
        trustLevel: { type: integer, minimum: 0, maximum: 6 }
        lastActiveAt: { type: integer, nullable: true }
        createdAt: { type: integer }
    AdminLifecycleJob:
      type: object
      required: [id, accountId, accountHandle, accountState, jobType, status, attempts, nextAttemptAt, createdAt, updatedAt]
      properties:
        id: { type: string }
        accountId: { type: string }
        accountHandle: { type: string }
        accountState: { $ref: "#/components/schemas/AccountLifecycleState" }
        jobType: { type: string, enum: [mark_deleted, purge] }
        status: { type: string, enum: [queued, running, succeeded, failed] }
        attempts: { type: integer, minimum: 0, maximum: 20 }
        nextAttemptAt: { type: integer }
        lockedAt: { type: integer, nullable: true }
        lastErrorCode: { type: string, nullable: true, maxLength: 80 }
        purgeStartedAt: { type: integer, nullable: true }
        createdAt: { type: integer }
        updatedAt: { type: integer }
    AdminUserInviteInput:
      type: object
      required: [email, handle, reason]
      properties:
        email: { type: string, format: email }
        handle: { type: string, minLength: 3, maxLength: 30 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AdminReasonInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
    MediaRetentionHoldInput:
      type: object
      additionalProperties: false
      required: [holdKind, expiresAt, reason, expectedHoldId]
      properties:
        holdKind: { type: string, enum: [moderation, security] }
        expiresAt:
          type: integer
          description: Unix seconds, at least five minutes and no more than 365 days in the future.
        reason: { type: string, minLength: 3, maxLength: 500 }
        expectedHoldId:
          type: string
          nullable: true
          description: Null creates only when no unreleased hold exists; a hold id renews exactly that reviewed record or returns conflict.
    MediaRetentionHoldReleaseInput:
      type: object
      additionalProperties: false
      required: [expectedHoldId, reason]
      properties:
        expectedHoldId: { type: string, description: The exact reviewed hold to release. }
        reason: { type: string, minLength: 3, maxLength: 500 }
    MediaRetentionHold:
      type: object
      description: Operations-only retention inventory record. Purpose text and staff identifiers are never exposed through the moderation queue.
      required: [id, uploadId, accountId, uploadStatus, holdKind, reason, placedBy, expiresAt, createdAt, isExpired]
      properties:
        id: { type: string }
        uploadId: { type: string }
        accountId: { type: string }
        uploadStatus: { type: string, enum: [pending, clean, quarantined, blocked] }
        holdKind: { type: string, enum: [moderation, security] }
        reason: { type: string }
        placedBy: { type: string }
        expiresAt: { type: integer, description: Unix seconds }
        createdAt: { type: integer, description: Unix seconds }
        isExpired: { type: boolean }
    MediaRetentionHoldPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/MediaRetentionHold" } } } }
    MediaDeletionJob:
      type: object
      description: Operations-only durable system deletion job. Provider object identifiers are deliberately omitted.
      required: [id, uploadId, accountId, uploadStatus, requestSource, reason, status, attemptCount, lastErrorCode, availableAt, createdAt, updatedAt]
      properties:
        id: { type: string }
        uploadId: { type: string }
        accountId: { type: string }
        uploadStatus: { type: string, enum: [pending, clean, quarantined, blocked] }
        requestSource: { type: string, enum: [retention_gc, account_purge, intent_cleanup] }
        reason: { type: string }
        status: { type: string, enum: [queued, leased, succeeded, dead_letter] }
        attemptCount: { type: integer, minimum: 0 }
        lastErrorCode: { type: string, nullable: true }
        availableAt: { type: integer, description: Unix seconds }
        createdAt: { type: integer, description: Unix seconds }
        updatedAt: { type: integer, description: Unix seconds }
    MediaDeletionJobPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/MediaDeletionJob" } } } }
    MediaReconciliationIssueCode:
      type: string
      description: Stable database-state anomaly category. Provider object inventory is reconciled separately.
      enum:
        - publication_missing
        - published_variant_set_incomplete
        - published_upload_not_clean
        - hidden_asset_has_published_variant
        - processing_lease_stale
        - processing_without_active_job
        - failed_publication_job_mismatch
        - deletion_lease_stale
        - cleanup_lease_stale
        - hidden_without_active_deletion_job
        - cleanup_plan_incomplete
        - deletion_completion_pending
        - deletion_dead_letter
        - processing_dead_letter
    MediaReconciliationFinding:
      type: object
      additionalProperties: false
      required: [assetId, issueCodes]
      properties:
        assetId: { type: string }
        issueCodes:
          type: array
          minItems: 1
          uniqueItems: true
          items: { $ref: "#/components/schemas/MediaReconciliationIssueCode" }
    MediaProviderInventoryStatus:
      type: object
      additionalProperties: false
      required: [state, ingestCandidateCount, deliveryCandidateCount]
      description: Database-side candidate counts only; it never claims that provider ListObjects inventory was inspected.
      properties:
        state: { type: string, enum: [manual_inventory_required] }
        ingestCandidateCount: { type: integer, format: int64, minimum: 0 }
        deliveryCandidateCount: { type: integer, format: int64, minimum: 0 }
    MediaReconciliationReport:
      type: object
      additionalProperties: false
      required: [dryRun, items, nextCursor, providerInventory]
      description: Bounded, audited and read-only PostgreSQL media-state inspection. It never mutates state or lists provider objects.
      properties:
        dryRun:
          type: boolean
          enum: [true]
          description: Always true; this endpoint never repairs findings.
        items:
          type: array
          items: { $ref: "#/components/schemas/MediaReconciliationFinding" }
        nextCursor:
          type: string
          nullable: true
          description: Last asset id when another bounded page of findings exists.
        providerInventory: { $ref: "#/components/schemas/MediaProviderInventoryStatus" }
    AdminForumFlag:
      type: object
      required: [id, targetType, targetId, reporterId, reason, weight, status, createdAt]
      properties:
        id: { type: string }
        targetType: { type: string, enum: [thread, comment] }
        targetId: { type: string }
        reporterId: { type: string }
        reason: { type: string, enum: [spam, abuse, off_topic, illegal, other] }
        note: { type: string, nullable: true }
        weight: { type: number }
        status: { type: string, enum: [open, upheld, rejected, ignored] }
        handledBy: { type: string, nullable: true }
        handledAt: { type: integer, nullable: true }
        resolutionNote: { type: string, nullable: true }
        authorHandle: { type: string, nullable: true }
        targetTitle: { type: string, nullable: true }
        contentExcerpt: { type: string, nullable: true }
        createdAt: { type: integer }
    AdminUserRoleInput:
      type: object
      required: [role, reason]
      properties:
        role: { type: string, enum: [user, mod] }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AdminOverview:
      type: object
      required: [totalUsers, activeUsers, suspendedUsers, pendingReviewReports, pendingForumFlags, pendingDmReports, pendingMediaUploads, threadsToday, commentsToday, likesToday]
      properties:
        totalUsers: { type: integer, minimum: 0 }
        activeUsers: { type: integer, minimum: 0 }
        suspendedUsers: { type: integer, minimum: 0 }
        moderators: { type: integer, minimum: 0 }
        administrators: { type: integer, minimum: 0 }
        pendingReviewReports: { type: integer, minimum: 0 }
        pendingForumFlags: { type: integer, minimum: 0 }
        pendingDmReports: { type: integer, minimum: 0 }
        pendingMediaUploads: { type: integer, minimum: 0 }
        threadsToday: { type: integer, minimum: 0 }
        commentsToday: { type: integer, minimum: 0 }
        likesToday: { type: integer, minimum: 0 }
    AdminAuditEvent:
      type: object
      required: [id, actorKind, action, targetType, targetId, createdAt]
      properties:
        id: { type: string }
        actorKind: { type: string, enum: [account, system, service] }
        actorId: { type: string, nullable: true }
        actorHandle: { type: string, nullable: true }
        actorRole: { type: string, nullable: true }
        action: { type: string }
        targetType: { type: string }
        targetId: { type: string }
        reason: { type: string, nullable: true }
        metadata: { type: object, nullable: true }
        createdAt: { type: integer }
    NotificationOutboxEvent:
      type: object
      additionalProperties: false
      required: [id, topic, recipientAccountId, eventType, state, attempts, maxAttempts, manualRetryCount, availableAt, createdAt, updatedAt]
      description: Operational metadata for a durable notification side effect. The private payload and source key are never exposed.
      properties:
        id: { type: string }
        topic: { type: string, enum: [notification, achievement_award] }
        recipientAccountId: { type: string }
        eventType: { type: string }
        state: { type: string, enum: [queued, running, succeeded, dead, cancelled] }
        attempts: { type: integer, minimum: 0 }
        maxAttempts: { type: integer, minimum: 1 }
        manualRetryCount: { type: integer, minimum: 0 }
        availableAt: { type: integer }
        lastErrorCode: { type: string, nullable: true }
        completedAt: { type: integer, nullable: true }
        deadAt: { type: integer, nullable: true }
        createdAt: { type: integer }
        updatedAt: { type: integer }
    NotificationOutboxRetryInput:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
    AchievementIcon:
      type: string
      enum: [award, book-open-check, message-circle-heart, star]
    AchievementStatus:
      type: string
      enum: [active, retired]
    Achievement:
      type: object
      required: [id, slug, name, description, icon, status, mintAmount, version, createdAt, updatedAt]
      properties:
        id: { type: string }
        slug: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        icon: { $ref: "#/components/schemas/AchievementIcon" }
        status: { $ref: "#/components/schemas/AchievementStatus" }
        mintAmount: { type: integer, minimum: 0 }
        version: { type: integer, minimum: 1 }
        createdAt: { type: integer }
        updatedAt: { type: integer }
    AchievementCreateInput:
      type: object
      additionalProperties: false
      required: [slug, name, icon, mintAmount, reason]
      properties:
        slug: { type: string, pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$", maxLength: 64 }
        name: { type: string, minLength: 1, maxLength: 100 }
        description: { type: string, maxLength: 240, nullable: true }
        icon: { $ref: "#/components/schemas/AchievementIcon" }
        mintAmount: { type: integer, minimum: 0, maximum: 100000 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AchievementUpdateInput:
      type: object
      additionalProperties: false
      required: [expectedVersion, name, icon, status, mintAmount, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        name: { type: string, minLength: 1, maxLength: 100 }
        description: { type: string, maxLength: 240, nullable: true }
        icon: { $ref: "#/components/schemas/AchievementIcon" }
        status: { $ref: "#/components/schemas/AchievementStatus" }
        mintAmount: { type: integer, minimum: 0, maximum: 100000 }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AchievementGrant:
      type: object
      required: [accountId, achievementId, slug, name, icon, definitionStatus, status, awardReason, awardedAt, awardedBy, revokedAt, revokedBy, revokeReason]
      properties:
        accountId: { type: string }
        achievementId: { type: string }
        slug: { type: string }
        name: { type: string }
        icon: { $ref: "#/components/schemas/AchievementIcon" }
        definitionStatus: { $ref: "#/components/schemas/AchievementStatus" }
        status: { type: string, enum: [active, revoked] }
        awardReason: { type: string, nullable: true }
        awardedAt: { type: integer }
        awardedBy: { type: string }
        revokedAt: { type: integer, nullable: true }
        revokedBy: { type: string, nullable: true }
        revokeReason: { type: string, nullable: true }
    AchievementGrantInput:
      type: object
      additionalProperties: false
      required: [achievementId, reason]
      properties:
        achievementId: { type: string }
        reason: { type: string, minLength: 3, maxLength: 500 }
    AchievementRevokeInput:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
    AchievementEvent:
      type: object
      required: [id, achievementId, slug, name, action, source, actorId, reason, createdAt]
      properties:
        id: { type: string }
        achievementId: { type: string }
        slug: { type: string }
        name: { type: string }
        action: { type: string, enum: [awarded, revoked] }
        source: { type: string, enum: [automatic, manual] }
        actorId: { type: string, nullable: true }
        reason: { type: string }
        createdAt: { type: integer }
    VerificationType:
      type: object
      required: [id, slug, category, label, icon, badgeVariant, allowsPublicDisplay, createdAt]
      properties:
        id: { type: string }
        slug: { type: string }
        category: { $ref: "#/components/schemas/VerificationCategory" }
        label: { type: string }
        description: { type: string, nullable: true }
        icon: { $ref: "#/components/schemas/VerificationIcon" }
        badgeVariant: { $ref: "#/components/schemas/VerificationBadgeVariant" }
        allowsPublicDisplay: { type: boolean }
        createdAt: { type: integer }
    VerificationTypeInput:
      type: object
      required: [slug, category, label, icon, badgeVariant, allowsPublicDisplay, reason]
      additionalProperties: false
      properties:
        slug: { type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$', minLength: 1, maxLength: 64 }
        category: { $ref: "#/components/schemas/VerificationCategory" }
        label: { type: string, minLength: 1, maxLength: 80 }
        description: { type: string, nullable: true, maxLength: 240 }
        icon: { $ref: "#/components/schemas/VerificationIcon" }
        badgeVariant: { $ref: "#/components/schemas/VerificationBadgeVariant" }
        allowsPublicDisplay: { type: boolean, default: false }
        reason: { type: string, minLength: 3, maxLength: 500 }
    VerificationGrant:
      type: object
      required: [id, accountId, verificationTypeId, slug, category, label, icon, badgeVariant, displayOnProfile, status, issuedBy, issuedAt, issueReason, hasEvidence]
      properties:
        id: { type: string }
        accountId: { type: string }
        verificationTypeId: { type: string }
        slug: { type: string }
        category: { $ref: "#/components/schemas/VerificationCategory" }
        label: { type: string }
        icon: { $ref: "#/components/schemas/VerificationIcon" }
        badgeVariant: { $ref: "#/components/schemas/VerificationBadgeVariant" }
        displayOnProfile: { type: boolean }
        status: { type: string, enum: [active, expired, revoked] }
        issuedBy: { type: string, nullable: true }
        issuedAt: { type: integer }
        expiresAt: { type: integer, nullable: true }
        issueReason: { type: string }
        hasEvidence: { type: boolean }
        revokedBy: { type: string, nullable: true }
        revokedAt: { type: integer, nullable: true }
        revokeReason: { type: string, nullable: true }
    VerificationGrantInput:
      type: object
      required: [verificationTypeId, displayOnProfile, reason]
      additionalProperties: false
      properties:
        verificationTypeId: { type: string }
        displayOnProfile: { type: boolean, default: false }
        expiresAt: { type: integer, nullable: true }
        evidenceReference:
          type: string
          nullable: true
          maxLength: 128
          description: Private opaque pointer to separately governed evidence; never returned by public or staff list responses.
        reason: { type: string, minLength: 3, maxLength: 500 }
    VerificationRevokeInput:
      type: object
      required: [reason]
      additionalProperties: false
      properties:
        reason: { type: string, minLength: 3, maxLength: 500 }
    FeatureThreadInput:
      type: object
      required: [featured, reason]
      properties:
        featured: { type: boolean }
        reason: { type: string, minLength: 3, maxLength: 500 }
    FeaturedThreadResponse:
      type: object
      properties:
        id: { type: string }
        featuredAt: { type: integer }
    # ---- typed pages (cont.) ----
    DmConversationPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/DmConversation" } } } }
    DmMessagePage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/DmMessage" } } } }
    DraftPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/DraftOutput" } } } }
    IgnorePage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/IgnoreUser" } } } }
    AchievementPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Achievement" } } } }
    AchievementEventPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AchievementEvent" } } } }
    AchievementGrantPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AchievementGrant" } } } }
    VerificationTypePage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/VerificationType" } } } }
    VerificationGrantPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/VerificationGrant" } } } }
    ActivityPolicyPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/ActivityPolicy" } } } }
    AdminUserPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AdminUser" } } } }
    AdminLifecycleJobPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AdminLifecycleJob" } } } }
    AdminAuditEventPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AdminAuditEvent" } } } }
    NotificationOutboxEventPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/NotificationOutboxEvent" } } } }
    DmReportPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/DmReport" } } } }
    AnnouncementPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Announcement" } } } }
    AnnouncementRevisionPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/AnnouncementRevision" } } } }
    PromotionPage:
      allOf:
        - $ref: "#/components/schemas/Page"
        - { type: object, properties: { items: { type: array, items: { $ref: "#/components/schemas/Promotion" } } } }

paths:
  /health:
    get:
      tags: [platform]
      security: []
      summary: Process liveness probe
      responses:
        "200": { description: alive, content: { application/json: { schema: { $ref: "#/components/schemas/HealthStatus" } } } }
  /ready:
    get:
      tags: [platform]
      security: []
      summary: Authoritative database and migration readiness probe
      responses:
        "200": { description: ready, content: { application/json: { schema: { $ref: "#/components/schemas/HealthStatus" } } } }
        "503": { description: not ready, content: { application/json: { schema: { $ref: "#/components/schemas/Error" } } } }
  # ============ auth & identity ============
  /auth/email/request-code:
    post:
      tags: [auth]
      security: []
      summary: Send a verification code to a @tongji.edu.cn address
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EmailCodeRequest" } } }
      responses:
        "204": { description: sent }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/email/verify:
    post:
      tags: [auth]
      security: []
      summary: Verify a code and log in / register
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/EmailCodeVerification" } } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AuthTokens" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "409": { $ref: "#/components/responses/Conflict" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/appeal/email/verify:
    post:
      tags: [auth]
      security: []
      summary: Exchange an appeal-purpose email code for an appeal-only credential
      description: Suspended accounts may use this credential only for their own appeal cases and governance notices.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AppealEmailVerification" } } } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AppealAccessToken" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /auth/appeal/password:
    post:
      tags: [auth]
      security: []
      summary: Verify campus email and password for appeal-only access
      description: Returns no refresh token and does not authorize any non-appeal route.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, format: password, minLength: 1, maxLength: 128 }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AppealAccessToken" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/recovery/password:
    post:
      tags: [auth]
      security: []
      summary: Prove control of a recoverable closed account with its password
      description: Returns only a short-lived recovery credential; no access session or refresh token is created. Ineligible accounts and wrong credentials share the same unauthorized response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, format: password, minLength: 1, maxLength: 128 }
      responses:
        "200": { description: recovery credential, content: { application/json: { schema: { $ref: "#/components/schemas/RecoveryCredential" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/recovery/email/verify:
    post:
      tags: [auth]
      security: []
      summary: Consume a recovery-purpose email code without opening a normal session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [email, code]
              properties:
                email: { type: string, format: email }
                code: { type: string, pattern: '^[0-9]{6}$' }
      responses:
        "200": { description: recovery credential, content: { application/json: { schema: { $ref: "#/components/schemas/RecoveryCredential" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /auth/recovery:
    get:
      tags: [auth]
      security: []
      summary: Inspect a purpose-bound recovery credential
      parameters:
        - { name: X-Recovery-Token, in: header, required: true, schema: { type: string, minLength: 43, maxLength: 43 } }
      responses:
        "200": { description: current recoverable lifecycle, content: { application/json: { schema: { $ref: "#/components/schemas/AccountLifecycle" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [auth]
      security: []
      summary: Reactivate a deactivated or not-yet-purged account
      description: Atomically consumes the recovery credential, revokes all old sessions, and returns the account to active without creating a session. The user must then sign in normally.
      parameters:
        - { name: X-Recovery-Token, in: header, required: true, schema: { type: string, minLength: 43, maxLength: 43 } }
      responses:
        "200": { description: reactivated lifecycle, content: { application/json: { schema: { $ref: "#/components/schemas/AccountLifecycle" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /auth/refresh:
    post:
      tags: [auth]
      security: []
      summary: Exchange a refresh token for new tokens
      description: Rotates the token exactly once. Reuse of a consumed token revokes its session family.
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/RefreshInput" } } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AuthTokens" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /auth/logout:
    post:
      tags: [auth]
      summary: Revoke the current device session family
      responses: { "204": { description: ok }, "401": { $ref: "#/components/responses/Unauthorized" } }
  /auth/logout-all:
    post:
      tags: [auth]
      summary: Revoke every session for the current account
      responses: { "204": { description: all sessions revoked }, "401": { $ref: "#/components/responses/Unauthorized" } }
  /auth/recent-auth:
    get:
      tags: [auth, identity]
      summary: Get current session recent-auth status
      description: Reads only the active server-side session timestamp; JWT iat is never accepted as freshness.
      responses:
        "200": { description: status, content: { application/json: { schema: { $ref: "#/components/schemas/RecentAuthStatus" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /auth/recent-auth/email/request-code:
    post:
      tags: [auth, identity]
      summary: Send a purpose-bound step-up code to the authenticated account
      description: The server resolves the current account's campus email; no email identifier is accepted or returned.
      responses:
        "204": { description: provider accepted the message }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/recent-auth/verify:
    post:
      tags: [auth, identity]
      summary: Refresh recent authentication for the current revocable session
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/RecentAuthVerifyInput" } } }
      responses:
        "200": { description: fresh status, content: { application/json: { schema: { $ref: "#/components/schemas/RecentAuthStatus" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
  /me:
    get:
      tags: [identity]
      summary: Current account
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Account" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    patch:
      tags: [identity]
      summary: Update the stable public handle
      requestBody:
        content: { application/json: { schema: { type: object, properties: { handle: { type: string } } } } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Account" } } } }
  /me/onboarding:
    get:
      tags: [identity]
      summary: Read resumable first-run onboarding state
      responses:
        "200": { description: onboarding state, content: { application/json: { schema: { $ref: "#/components/schemas/OnboardingState" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      tags: [identity]
      summary: Complete required handle, profile, privacy and current-terms choices atomically
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/OnboardingCompleteInput" } } }
      responses:
        "200": { description: completed onboarding state, content: { application/json: { schema: { $ref: "#/components/schemas/OnboardingState" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }
  /me/lifecycle:
    get:
      tags: [identity]
      summary: Read the authenticated account lifecycle state
      responses:
        "200": { description: lifecycle, content: { application/json: { schema: { $ref: "#/components/schemas/AccountLifecycle" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/lifecycle/deactivate:
    post:
      tags: [identity]
      summary: Deactivate the current account and revoke every session
      description: Requires server-side recent authentication. The response contains a purpose-bound recovery credential so a lost response is not the only recovery path; password/email recovery remains available.
      parameters: [{ $ref: "#/components/parameters/ReconciliationIdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DeactivateAccountInput" } } } }
      responses:
        "200": { description: deactivated, content: { application/json: { schema: { $ref: "#/components/schemas/AccountLifecycleMutation" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
        "409": { $ref: "#/components/responses/Conflict" }
  /me/lifecycle/delete:
    post:
      tags: [identity]
      summary: Request account deletion with a 30-day recovery window
      description: Requires server-side recent authentication, immediately closes public/new-interaction access, revokes every session, and enqueues durable deletion and purge stages.
      parameters: [{ $ref: "#/components/parameters/ReconciliationIdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DeleteAccountInput" } } } }
      responses:
        "202": { description: deletion requested, content: { application/json: { schema: { $ref: "#/components/schemas/AccountLifecycleMutation" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
        "409": { $ref: "#/components/responses/Conflict" }
  /me/data-exports:
    get:
      tags: [identity]
      summary: List up to 20 recent owner-data export jobs
      responses:
        "200":
          description: recent export jobs, newest first
          content:
            application/json:
              schema: { type: array, maxItems: 20, items: { $ref: "#/components/schemas/DataExportJob" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [identity]
      summary: Create or return an idempotent owner-data export job
      description: Requires server-side recent authentication. The durable job is assembled through owner-domain projections and expires after 24 hours.
      parameters:
        - { $ref: "#/components/parameters/ReconciliationIdempotencyKey" }
      responses:
        "202": { description: export queued or existing, content: { application/json: { schema: { $ref: "#/components/schemas/DataExportJob" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /me/data-exports/{id}:
    get:
      tags: [identity]
      summary: Read an owner data-export job status
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: export status, content: { application/json: { schema: { $ref: "#/components/schemas/DataExportJob" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /me/data-exports/{id}/download-grant:
    post:
      tags: [identity]
      summary: Issue a five-minute one-time download grant for a ready owner export
      description: Requires server-side recent authentication so a later session compromise cannot fetch an earlier export artifact.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: download grant, content: { application/json: { schema: { $ref: "#/components/schemas/DataExportDownloadGrant" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /me/data-exports/{id}/download:
    get:
      tags: [identity]
      summary: Consume a one-time grant and download the machine-readable owner export
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: X-Export-Token, in: header, required: true, schema: { type: string, minLength: 43, maxLength: 43 } }
      responses:
        "200": { description: JSON export, content: { application/json: { schema: { $ref: "#/components/schemas/AccountDataExport" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /me/profile:
    get:
      tags: [identity]
      summary: Get owner-editable public profile fields
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/MyProfile" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      tags: [identity]
      summary: Replace owner-editable text profile fields
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/ProfileUpdateInput" } } }
      responses:
        "200": { description: saved, content: { application/json: { schema: { $ref: "#/components/schemas/MyProfile" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/privacy:
    get:
      tags: [identity]
      summary: Get profile, activity, relationship-list, discovery, new-DM, and mention privacy settings
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ProfilePrivacy" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      tags: [identity]
      summary: Replace profile privacy settings
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/ProfilePrivacyUpdateInput" } } }
      responses:
        "200": { description: saved, content: { application/json: { schema: { $ref: "#/components/schemas/ProfilePrivacy" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/sessions:
    get:
      tags: [identity]
      summary: List active device sessions
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/SessionPage" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/sessions/revoke-others:
    post:
      tags: [identity]
      summary: Revoke every session except the current one
      responses:
        "204": { description: other sessions revoked }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/sessions/{id}:
    delete:
      tags: [identity]
      summary: Revoke one device session
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "204": { description: session revoked }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /me/activity:
    get:
      tags: [activity]
      summary: Current user's daily public activity calendar
      description: Returns every Asia/Shanghai calendar day in the requested inclusive range, including zero-count days. The maximum range is 371 days.
      parameters:
        - { name: from, in: query, schema: { type: string, format: date } }
        - { name: to, in: query, schema: { type: string, format: date } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ActivityCalendar" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/check-in:
    get:
      tags: [activity]
      summary: Read today's idempotent daily check-in status
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CheckInStatus" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [activity]
      summary: Check in once for the current Asia/Shanghai day
      description: Idempotent; a repeated request returns the existing check-in without adding activity again.
      responses:
        "200": { description: current status, content: { application/json: { schema: { $ref: "#/components/schemas/CheckInStatus" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /me/trust-progress:
    get:
      tags: [activity]
      summary: Current user's unified trust level progress
      description: Returns the authoritative tea trust level, live qualifying score, next threshold, and override state.
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/TrustProgress" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/drafts:
    get:
      tags: [forum]
      summary: List my cross-device forum drafts
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/DraftPage" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      tags: [forum]
      summary: Create or compare-and-swap a typed forum draft
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DraftSaveInput" } } } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/DraftOutput" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }
  /me/drafts/{draft_key}:
    get:
      tags: [forum]
      summary: Get one account-owned draft by key
      parameters: [{ name: draft_key, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/DraftOutput" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [forum]
      summary: Idempotently delete one account-owned draft
      parameters: [{ name: draft_key, in: path, required: true, schema: { type: string } }]
      responses:
        "204": { description: deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/ignores/{account_id}:
    put:
      tags: [identity]
      deprecated: true
      summary: Compatibility alias for blocking a user by account id
      parameters: [{ name: account_id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: ok } }
    delete:
      tags: [identity]
      deprecated: true
      summary: Compatibility alias for unblocking a user by account id
      parameters: [{ name: account_id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: ok } }
  /me/ignores:
    get:
      tags: [identity]
      deprecated: true
      summary: Compatibility alias for the current account's block list
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/IgnorePage" } } } } }
  /users/{handle}:
    get:
      tags: [identity]
      security: [{ bearer: [] }, {}]
      summary: User profile subject to the account's visibility policy
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UserProfile" } } } }, "404": { $ref: "#/components/responses/NotFound" } }
  /users/{handle}/threads:
    get:
      tags: [identity]
      security: [{ bearer: [] }, {}]
      summary: User's visible threads subject to profile, activity visibility, and block rules
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UserThreadPage" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
  /users/{handle}/comments:
    get:
      tags: [identity]
      security: [{ bearer: [] }, {}]
      summary: User's visible comments subject to profile, activity visibility, and block rules
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UserCommentPage" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
  /users/{handle}/media:
    get:
      tags: [forum]
      security: [{ bearer: [] }, {}]
      summary: User's authored visible Forum content with at least one current clean image
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ProfileContentPage" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
  /users/{handle}/likes:
    get:
      tags: [forum]
      security: [{ bearer: [] }, {}]
      summary: User's positive Forum votes subject to profile activity visibility and relationship rules
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ProfileContentPage" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
  /users/{handle}/relationship:
    get:
      tags: [identity]
      summary: Current account's social relationship with an active user
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UserRelationship" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /users/{handle}/follow:
    put:
      tags: [identity]
      summary: Follow a public account; no approval state is created
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: following }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
    delete:
      tags: [identity]
      summary: Unfollow an account idempotently
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: not following }, "404": { $ref: "#/components/responses/NotFound" } }
  /me/followers/{handle}:
    delete:
      tags: [identity]
      summary: Remove an account from the authenticated user's followers without blocking it
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses:
        "204": { description: follower removed or relationship already absent }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /users/{handle}/followers:
    get:
      tags: [identity]
      security: [{ bearer: [] }, {}]
      summary: Followers subject to the owner's list visibility and member discoverability
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UserSummaryPage" } } } }, "404": { $ref: "#/components/responses/NotFound" } }
  /users/{handle}/following:
    get:
      tags: [identity]
      security: [{ bearer: [] }, {}]
      summary: Followed accounts subject to the owner's list visibility and member discoverability
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UserSummaryPage" } } } }, "404": { $ref: "#/components/responses/NotFound" } }
  /users/{handle}/mute:
    put:
      tags: [identity]
      summary: Privately hide an account's content without changing access or follow state
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: muted }, "404": { $ref: "#/components/responses/NotFound" } }
    delete:
      tags: [identity]
      summary: Unmute an account idempotently
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: not muted }, "404": { $ref: "#/components/responses/NotFound" } }
  /users/{handle}/block:
    put:
      tags: [identity]
      summary: Create a bilateral safety boundary and remove follows in both directions
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: blocked }, "404": { $ref: "#/components/responses/NotFound" } }
    delete:
      tags: [identity]
      summary: Unblock without restoring prior follows
      parameters: [{ name: handle, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: not blocked }, "404": { $ref: "#/components/responses/NotFound" } }

  # ============ wallet (bind / claim / ledger) ============
  /wallet:
    get:
      tags: [wallet]
      summary: Balance
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Wallet" } } } } }
  /wallet/bind:
    post:
      tags: [wallet]
      summary: Enroll the first client-generated Ed25519 public key
      description: Requires fresh session-bound authentication. Repeating the canonical active key is idempotent; enrolling a different key while one is active is rejected because key rotation requires a separately designed old-key proof or audited recovery flow.
      requestBody:
        required: true
        content: { application/json: { schema: { type: object, required: [publicKey], properties: { publicKey: { type: string, description: Standard base64 encoding of a 32-byte Ed25519 public key } } } } }
      responses:
        "204": { description: enrolled or already the canonical active key }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /wallet/claim-challenge:
    get:
      tags: [wallet]
      summary: Get a one-time claim challenge
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/WalletClaimChallenge" } } } } }
  /wallet/claim:
    post:
      tags: [wallet]
      summary: Claim a legacy wallet by signing the challenge (re-derive userHash from 学号+PIN)
      requestBody:
        required: true
        content: { application/json: { schema: { type: object, required: [legacyUserHash, challengeId, signature], properties: { legacyUserHash: { type: string }, challengeId: { type: string }, signature: { type: string } } } } }
      responses:
        "200": { description: merged, content: { application/json: { schema: { $ref: "#/components/schemas/Wallet" } } } }
        "409": { $ref: "#/components/responses/Conflict" }
  /wallet/ledger:
    get:
      tags: [wallet]
      summary: My ledger history (cursor)
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/LedgerPage" } } } } }
  /wallet/ledger/verify:
    get:
      tags: [wallet]
      security: []
      summary: Recompute the ledger hash chain and verify signatures
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/LedgerVerify" } } } } }

  # ============ credit: controlled money ops (NO free transfer — design §6.3) ============
  /credit/signing-intents:
    post:
      tags: [credit]
      summary: Create a request- and snapshot-bound one-time wallet signing intent
      parameters: [{ $ref: "#/components/parameters/WalletIdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/SigningIntentInput" } } } }
      responses:
        "200": { description: exact bytes to sign, content: { application/json: { schema: { $ref: "#/components/schemas/SigningIntent" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /credit/tip:
    post:
      tags: [credit]
      summary: Tip — controlled transfer bound to a specific piece of content
      parameters: [{ $ref: "#/components/parameters/WalletIntent" }, { $ref: "#/components/parameters/WalletSig" }, { $ref: "#/components/parameters/WalletIdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/TipInput" } } } }
      responses:
        "204": { description: tipped }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
  # NOTE: /credit/bounty and /credit/bounty/{id}/confirm are aliases for
  # /credit/tasks POST and /credit/tasks/{id}/action confirm; backend only
  # exposes the canonical /credit/tasks routes.

  # ============ password auth ============
  /auth/password/login:
    post:
      tags: [auth]
      security: []
      summary: Campus email + password login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, format: password, minLength: 1, maxLength: 128 }
                clientInstallationId: { $ref: "#/components/schemas/ClientInstallationId" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AuthTokens" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/password/forgot:
    post:
      tags: [auth]
      security: []
      summary: Request a password-reset code
      description: Always returns the same successful response for valid campus-email syntax, whether or not an eligible password account exists.
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [email, captchaToken], properties: { email: { type: string, format: email }, captchaToken: { type: string, minLength: 1 } } } } } }
      responses:
        "204": { description: Request accepted without revealing account eligibility or provider delivery }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/password/reset:
    post:
      tags: [auth]
      security: []
      summary: Reset password with code
      description: Performs a neutral proof preflight, then atomically re-verifies and consumes a credential-version-bound reset code, replaces every prior session with one new session, and returns its token pair. Missing, ineligible, expired, and invalid-code subjects share the same 400 response.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PasswordResetInput" }
      responses:
        "200": { description: Password reset and replacement session issued, content: { application/json: { schema: { $ref: "#/components/schemas/AuthTokens" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }
  /auth/password/set:
    post:
      tags: [auth]
      summary: Set the authenticated code-only account's first password
      description: Requires recent authentication, succeeds only while no password exists, and atomically replaces all prior sessions with one new session.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PasswordSetInput" } } } }
      responses:
        "200": { description: Password set and replacement session issued, content: { application/json: { schema: { $ref: "#/components/schemas/AuthTokens" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /auth/password/change:
    post:
      tags: [auth]
      summary: Change the authenticated account's password
      description: Requires the current password and atomically replaces every prior refresh family, including the current one, with one new session.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PasswordChangeInput" }
      responses:
        "200": { description: Password changed and replacement session issued, content: { application/json: { schema: { $ref: "#/components/schemas/AuthTokens" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }


  # ---- escrow market: tasks ----
  /credit/tasks:
    get:
      tags: [credit]
      summary: Task square / my tasks
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [open, in_progress, submitted, completed, cancelled, all] } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/TaskPage" } } } } }
    post:
      tags: [credit]
      summary: Publish a task (escrow_hold of the reward)
      parameters: [{ $ref: "#/components/parameters/WalletIntent" }, { $ref: "#/components/parameters/WalletSig" }, { $ref: "#/components/parameters/WalletIdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/TaskInput" } } } }
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Task" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /credit/tasks/{id}/accept:
    post:
      tags: [credit]
      summary: Accept a task
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: accepted }, "400": { $ref: "#/components/responses/BadRequest" }, "409": { $ref: "#/components/responses/Conflict" } }
  /credit/tasks/{id}/action:
    post:
      tags: [credit]
      summary: Two-phase confirm — submit / confirm(release) / cancel / reject / delete
      description: confirm/cancel/reject and an open-task delete require wallet intent, signature and idempotency headers; submit and deletion after cancellation do not move value.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/OptionalWalletIntent" }, { $ref: "#/components/parameters/OptionalWalletSig" }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/TaskAction" } } } }
      responses: { "204": { description: updated }, "400": { $ref: "#/components/responses/BadRequest" }, "409": { $ref: "#/components/responses/Conflict" } }

  # ---- escrow market: products / purchases ----
  /credit/products:
    get:
      tags: [credit]
      summary: Product listings
      parameters: [{ name: status, in: query, schema: { type: string, enum: [on_sale, off_sale, sold_out, all] } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ProductPage" } } } } }
    post:
      tags: [credit]
      summary: List a product
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ProductInput" } } } }
      responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Product" } } } } }
  /credit/products/{id}/purchase:
    post:
      tags: [credit]
      summary: Buy a product (escrow_hold of the price)
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/WalletIntent" }, { $ref: "#/components/parameters/WalletSig" }, { $ref: "#/components/parameters/WalletIdempotencyKey" }]
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Purchase" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /credit/purchases:
    get:
      tags: [credit]
      summary: My orders (as buyer or seller)
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/PurchasePage" } } } } }
  /credit/purchases/{id}/action:
    post:
      tags: [credit]
      summary: Escrow steps — accept / deliver / confirm(release) / cancel(refund)
      description: confirm and cancel require wallet intent, signature and idempotency headers; accept and deliver are non-value seller transitions.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/OptionalWalletIntent" }, { $ref: "#/components/parameters/OptionalWalletSig" }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PurchaseAction" } } } }
      responses: { "204": { description: updated }, "400": { $ref: "#/components/responses/BadRequest" }, "409": { $ref: "#/components/responses/Conflict" } }

  # ============ search ============
  /search:
    get:
      tags: [search]
      security: [{}, { bearer: [] }]
      summary: Privacy-safe typed federated search across catalogue and community objects
      description: Meilisearch ranks candidate ids. Owner domains rehydrate and authorize every hit before canonical highlight ranges or conservative spelling suggestions are computed.
      parameters:
        - { name: q, in: query, required: true, schema: { type: string, minLength: 2, maxLength: 100 } }
        - { name: type, in: query, schema: { type: string, enum: [course, teacher, review, thread, user, board, tag, all], default: all } }
        - { name: limit, in: query, schema: { type: integer, default: 10, minimum: 1, maximum: 30 } }
        - name: cursor
          in: query
          schema: { type: string }
          description: Opaque cursor returned for the same q and a specific non-all type; all-scope pagination uses hasMoreScopes then switches type.
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/SearchResult" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "429": { $ref: "#/components/responses/RateLimited" }

  # ============ courses ============
  /departments:
    get:
      tags: [courses]
      security: []
      summary: Departments
      responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Department" } } } } } }
  /courses:
    get:
      tags: [courses]
      security: []
      summary: Browse / filter courses (cached; non-realtime)
      parameters:
        - { name: dept, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string, enum: [hot, rating, new] } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CoursePage" } } } } }
  /courses/{id}:
    get:
      tags: [courses]
      security: []
      summary: Course detail
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CourseDetail" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
  /courses/{id}/related:
    get:
      tags: [courses]
      security: []
      summary: Related courses
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Course" } } } } } }
  /courses/by-code/{code}:
    get:
      tags: [courses]
      security: []
      summary: Lookup by code
      parameters: [{ name: code, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CourseDetail" } } } } }
  /courses/{id}/ai-summary:
    get:
      tags: [courses]
      security: []
      summary: AI-generated review summary for a course
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AiSummary" } } } } }

  # ============ selection (选课/PK) ============
  /selection/calendars:
    get: { tags: [selection], security: [], summary: All calendars, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Calendar" } } } } } } }
  /selection/campuses:
    get: { tags: [selection], security: [], summary: All campuses, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Campus" } } } } } } }
  /selection/faculties:
    get: { tags: [selection], security: [], summary: All faculties, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Faculty" } } } } } } }
  /selection/grades:
    get:
      tags: [selection]
      security: []
      summary: Grades for a calendar
      parameters: [{ $ref: "#/components/parameters/SelectionCalendarId" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { type: array, items: { type: string } } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /selection/majors:
    get:
      tags: [selection]
      security: []
      summary: Majors with teaching classes in a calendar and grade
      parameters: [{ $ref: "#/components/parameters/SelectionCalendarId" }, { name: grade, in: query, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Major" } } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /selection/course-natures:
    get: { tags: [selection], security: [], summary: Optional course types/natures, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/CourseNature" } } } } } } }
  /selection/courses-by-major:
    get:
      tags: [selection]
      security: []
      summary: Teaching classes for a major in a calendar
      parameters: [{ $ref: "#/components/parameters/SelectionCalendarId" }, { name: majorId, in: query, required: true, schema: { type: string } }, { name: grade, in: query, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/SelectionCourse" } } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /selection/courses-by-nature:
    get:
      tags: [selection]
      security: []
      summary: Teaching classes by nature/type in a calendar
      parameters: [{ $ref: "#/components/parameters/SelectionCalendarId" }, { name: natureId, in: query, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/SelectionCourse" } } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /selection/courses/{teachingClassId}:
    get:
      tags: [selection]
      security: []
      summary: Selection teaching-class detail by stable identifier
      parameters: [{ $ref: "#/components/parameters/TeachingClassId" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/SelectionCourse" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
  /selection/courses/search:
    get:
      tags: [selection]
      security: []
      summary: Search selection teaching classes in a calendar
      parameters: [{ $ref: "#/components/parameters/SelectionCalendarId" }, { name: q, in: query, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/SelectionCourse" } } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
  /selection/courses/{teachingClassId}/timeslots:
    get:
      tags: [selection]
      security: []
      summary: Timeslots for a stable teaching-class identifier
      parameters: [{ $ref: "#/components/parameters/TeachingClassId" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/TimeSlot" } } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
  /selection/latest-update:
    get: { tags: [selection], security: [], summary: Latest sync time, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/LatestUpdate" } } } } } }

  # ============ reviews ============
  /courses/{id}/reviews:
    get:
      tags: [reviews]
      security: [{}, { bearer: [] }]
      summary: Course reviews (paginated, sorted)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string, enum: [hot, new], default: hot } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ReviewPage" } } } } }
    post:
      tags: [reviews]
      summary: Publish a review (captcha + idempotency)
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/IdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateReviewInput" } } } }
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Review" } } } }
        "409": { $ref: "#/components/responses/Conflict" }
  /reviews/{id}:
    get:
      tags: [reviews]
      security: [{}, { bearer: [] }]
      summary: Get one visible review with viewer state
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Review" } } } }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [reviews]
      summary: Edit own review
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { content: { application/json: { schema: { $ref: "#/components/schemas/ReviewInput" } } } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Review" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
  /reviews/{id}/like:
    post: { tags: [reviews], summary: Like, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "204": { description: ok } } }
    delete: { tags: [reviews], summary: Unlike, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "204": { description: ok } } }
  /reviews/{id}/report:
    post:
      tags: [reviews]
      summary: Report a visible review authored by another account
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [reason, captchaToken], properties: { reason: { type: string, minLength: 3, maxLength: 500 }, captchaToken: { type: string, minLength: 1 } } } } } }
      responses: { "204": { description: reported }, "400": { $ref: "#/components/responses/BadRequest" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }

  # ============ forum (F0 + F1) ============
  /forum/boards:
    get: { tags: [forum], security: [{}, { bearer: [] }], summary: List boards with optional viewer posting access, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Board" } } } } } } }
  /forum/tags:
    get: { tags: [forum], security: [], summary: List tags, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Tag" } } } } } } }
  /forum/threads:
    get:
      tags: [forum]
      security: [{}, { bearer: [] }]
      summary: Thread feed (hot/new/subscriptions/following/unread)
      parameters:
        - { name: board, in: query, schema: { type: string } }
        - { name: tag, in: query, description: Exact tag slug, schema: { type: string, minLength: 1, maxLength: 64 } }
        - { name: sort, in: query, schema: { type: string, enum: [hot, new, subscriptions, following, unread], default: new } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadFeedPage" } } } } }
    post:
      tags: [forum]
      summary: Create thread with board, trust-level, sanction, and content-policy checks
      description: Ordinary accounts require an unlocked board and trustLevel >= minTrustToPost. Accounts with moderation.content may bypass only those two board gates.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadInput" } } } }
      responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadDetail" } } } } }
  /forum/boards/{boardId}/threads:
    get:
      tags: [forum]
      security: [{}, { bearer: [] }]
      summary: List visible threads in one board
      parameters:
        - { name: boardId, in: path, required: true, schema: { type: string } }
        - { name: tag, in: query, description: Exact tag slug, schema: { type: string, minLength: 1, maxLength: 64 } }
        - { name: sort, in: query, schema: { type: string, enum: [hot, new], default: new } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadFeedPage" } } } } }
  /forum/threads/{id}:
    get: { tags: [forum], security: [{}, { bearer: [] }], summary: Thread detail with optional viewer state, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadDetail" } } } }, "404": { $ref: "#/components/responses/NotFound" } } }
    patch: { tags: [forum], summary: Compare-and-swap edit thread, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadUpdateInput" } } } }, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadDetail" } } } }, "409": { $ref: "#/components/responses/Conflict" } } }
    delete: { tags: [forum], summary: Soft-delete thread, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "200": { description: deleted, content: { application/json: { schema: { type: object, required: [ok], properties: { ok: { type: boolean } } } } } } } }
  /forum/threads/{id}/read:
    post: { tags: [forum], summary: Report read position, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ReadTrackingInput" } } } }, responses: { "204": { description: ok } } }
  /forum/threads/{id}/revisions:
    get:
      tags: [forum]
      summary: Bounded thread revision history
      description: The author may read their own history. Staff may read only another strictly lower-role author's history.
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/RevisionPage" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /forum/threads/{id}/comments:
    get: { tags: [forum], security: [{}, { bearer: [] }], summary: Comments with optional viewer state, parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }], responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CommentPage" } } } } } }
    post: { tags: [forum], summary: Add comment, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CommentInput" } } } }, responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Comment" } } } } } }
  /forum/comments/{id}:
    patch: { tags: [forum], summary: Compare-and-swap edit comment, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CommentUpdateInput" } } } }, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Comment" } } } }, "409": { $ref: "#/components/responses/Conflict" } } }
    delete: { tags: [forum], summary: Soft-delete comment, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "200": { description: deleted, content: { application/json: { schema: { type: object, required: [ok], properties: { ok: { type: boolean } } } } } } } }
  /forum/comments/{id}/revisions:
    get:
      tags: [forum]
      summary: Bounded comment revision history
      description: The author may read their own history. Staff may read only another strictly lower-role author's history.
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/RevisionPage" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /forum/posts/{id}/vote:
    post: { tags: [forum], summary: Up/down vote, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/VoteInput" } } } }, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/VoteResponse" } } } } } }
    delete:
      tags: [forum]
      summary: Remove the current account's vote
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: postType, in: query, required: true, schema: { type: string, enum: [thread, comment] } }
      responses: { "200": { description: removed or already absent, content: { application/json: { schema: { $ref: "#/components/schemas/VoteResponse" } } } } }
  /forum/posts/{id}/flag:
    post: { tags: [forum], summary: Flag post, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FlagInput" } } } }, responses: { "200": { description: queued, content: { application/json: { schema: { $ref: "#/components/schemas/FlagResponse" } } } } } }
  /forum/posts/{id}/bookmark:
    put: { tags: [forum], summary: Bookmark, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/BookmarkInput" } } } }, responses: { "204": { description: ok } } }
    delete:
      tags: [forum]
      summary: Remove bookmark
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: postType, in: query, required: true, schema: { type: string, enum: [thread, comment] } }
      responses: { "204": { description: removed or already absent } }
  /forum/bookmarks: { get: { tags: [forum], summary: List the current account's bookmarks with visible content projections, parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }], responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/BookmarkPage" } } } } } } }
  /forum/subscriptions:
    put: { tags: [forum], summary: Set subscription, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/SubscriptionInput" } } } }, responses: { "204": { description: ok } } }
    delete: { tags: [forum], summary: Remove subscription, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UnsubscribeInput" } } } }, responses: { "204": { description: ok } } }
    get: { tags: [forum], summary: List subscriptions, parameters: [{ name: type, in: query, schema: { type: string, enum: [board, thread] } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }], responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/SubscriptionPage" } } } } } }
  /forum/threads/unread:
    get:
      tags: [forum]
      summary: List unread threads
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadFeedPage" } } } } }
  /forum/comments/{id}/solve:
    post:
      tags: [forum]
      summary: Mark comment as solved answer (Q&A boards, thread author or mod)
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: ok } }
    delete:
      tags: [forum]
      summary: Unmark solved answer
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: ok } }
  /forum/dm/conversations:
    get:
      tags: [forum]
      summary: List DM conversations
      parameters:
        - { name: view, in: query, schema: { type: string, enum: [inbox, requests, sent, archived, deleted], default: inbox } }
        - { name: q, in: query, schema: { type: string, minLength: 2, maxLength: 100 } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/DmConversationPage" } } } } }
    post:
      tags: [forum]
      summary: Create or get a direct conversation or bounded message request
      parameters:
        - { name: Idempotency-Key, in: header, required: false, schema: { type: string, minLength: 1, maxLength: 128 } }
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DmConversationInput" } } } }
      responses:
        "200": { description: direct conversation or pending request, content: { application/json: { schema: { $ref: "#/components/schemas/DmConversation" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "429": { $ref: "#/components/responses/RateLimited" }
  /forum/dm/unread-count:
    get:
      tags: [forum]
      summary: Count unread private messages across active conversations
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DmCounts" }
  /forum/dm/requests/{id}/accept:
    post:
      tags: [forum]
      summary: Accept an incoming message request
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: accepted conversation, content: { application/json: { schema: { $ref: "#/components/schemas/DmConversation" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /forum/dm/requests/{id}:
    delete:
      tags: [forum]
      summary: Decline an incoming request or withdraw an outgoing request without blocking or notifying
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: removed }, "404": { $ref: "#/components/responses/NotFound" } }
  /forum/dm/requests/{id}/report:
    post:
      tags: [forum]
      summary: Report the one request message and remove the incoming request
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DmReportInput" } } } }
      responses: { "202": { description: queued and request removed }, "404": { $ref: "#/components/responses/NotFound" } }
  /forum/dm/conversations/{id}:
    delete:
      tags: [forum]
      summary: Hide a conversation for the current participant
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: hidden for this participant }, "404": { $ref: "#/components/responses/NotFound" } }
  /forum/dm/conversations/{id}/recover:
    post:
      tags: [forum]
      summary: Recover a participant-hidden conversation
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: recovered }, "404": { $ref: "#/components/responses/NotFound" } }
  /forum/dm/conversations/{id}/archive:
    put:
      tags: [forum]
      summary: Archive a conversation for the current participant
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: archived }, "404": { $ref: "#/components/responses/NotFound" } }
    delete:
      tags: [forum]
      summary: Return an archived conversation to the inbox
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: unarchived }, "404": { $ref: "#/components/responses/NotFound" } }
  /forum/dm/conversations/{id}/mute:
    put:
      tags: [forum]
      summary: Mute notifications for a conversation
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: muted }, "404": { $ref: "#/components/responses/NotFound" } }
    delete:
      tags: [forum]
      summary: Restore notifications for a conversation
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "204": { description: unmuted }, "404": { $ref: "#/components/responses/NotFound" } }
  /forum/dm/conversations/{id}/messages:
    get:
      tags: [forum]
      summary: List messages in a conversation
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/DmMessagePage" } } } } }
    post:
      tags: [forum]
      summary: Send or safely replay a message
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DmMessageInput" } } } }
      responses:
        "201": { description: created or safely replayed, content: { application/json: { schema: { $ref: "#/components/schemas/DmMessage" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "409": { $ref: "#/components/responses/Conflict" }
  /forum/dm/conversations/{id}/read:
    post:
      tags: [forum]
      summary: Mark a DM conversation read through a message
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { content: { application/json: { schema: { $ref: "#/components/schemas/DmReadInput" } } } }
      responses: { "204": { description: read position updated }, "404": { $ref: "#/components/responses/NotFound" } }
  /forum/dm/messages/{id}/report:
    post:
      tags: [forum]
      summary: Report a private message to moderators
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DmReportInput" } } } }
      responses: { "202": { description: queued }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /forum/polls/{id}/vote:
    post:
      tags: [forum]
      summary: Vote on a poll option
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [optionId], properties: { optionId: { type: string } } } } } }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/PollVoteResponse" } } } }, "409": { $ref: "#/components/responses/Conflict" } }
    delete:
      tags: [forum]
      summary: Remove the current account's vote for one poll option
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: optionId, in: query, required: true, schema: { type: string } }
      responses: { "200": { description: removed or already absent, content: { application/json: { schema: { $ref: "#/components/schemas/PollVoteResponse" } } } }, "409": { $ref: "#/components/responses/Conflict" } }
  /forum/polls/{id}/results:
    get:
      tags: [forum]
      security: [{}, { bearer: [] }]
      summary: Get poll results
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Poll" } } } } }

  # ============ media ==========
  /media/upload-credentials:
    post:
      tags: [media]
      summary: Issue an OSS upload intent and scoped STS credentials
      description: The server enforces bounded active intents, rolling daily issuance, live-object, retained-record, and reserved-byte quotas before requesting exact-key STS credentials.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UploadIntentInput" } } } }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UploadCredentials" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }
  /media/callback:
    post:
      tags: [media]
      security: []
      summary: Alibaba OSS signed upload callback
      description: Verifies the provider signature, one-time callback token, exact object metadata, and upload intent before atomically creating the upload. When the runtime image auto-approval policy is enabled, supported JPEG/PNG/WebP uploads enter sanitized Delivery processing with a system audit event; all other uploads remain pending. No callback result is publicly deliverable until publication completes.
      requestBody: { required: true, content: { application/json: { schema: { type: object } } } }
      responses:
        "200": { description: ok }
        "400": { $ref: "#/components/responses/BadRequest" }
  /media/{id}/url:
    get:
      tags: [media]
      summary: Get the owner's short-lived published CDN delivery projection
      description: Owner-only compatibility route. It returns a clean, atomically published sanitized server-owned variant; pending preview uses the separate same-origin no-store endpoint. Public business surfaces receive Media projections through their owning API rather than calling this route.
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - name: variant
          in: query
          required: false
          description: Existing sanitized variant to project; defaults to display_1280 for backward compatibility.
          schema:
            allOf: [{ $ref: "#/components/schemas/MediaDeliveryVariant" }]
      responses:
        "200": { description: short-lived CDN projection, content: { application/json: { schema: { $ref: "#/components/schemas/MediaDelivery" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /me/media/uploads:
    get:
      tags: [media]
      summary: List the current account's recent uploads for moderation and processing recovery
      parameters:
        - name: usage
          in: query
          schema: { $ref: "#/components/schemas/MediaUsage" }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/MyUploadPage" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/media/uploads/{id}:
    get:
      tags: [media]
      summary: Poll one owned upload's moderation and Delivery status
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/MyUpload" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /me/media/uploads/{id}/preview:
    get:
      tags: [media]
      summary: Stream the owner's bounded pending raster preview
      description: Owner-only same-origin response. It is MIME/size/pixel bounded, private no-store, never discloses a provider URL, and becomes unavailable after moderation leaves pending.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      responses:
        "200":
          description: bounded pending image stream
          headers:
            Cache-Control: { schema: { type: string } }
            X-Content-Type-Options: { schema: { type: string } }
          content:
            image/jpeg: { schema: { type: string, format: binary } }
            image/png: { schema: { type: string, format: binary } }
            image/webp: { schema: { type: string, format: binary } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /me/profile/avatar:
    put:
      tags: [media]
      summary: Bind an owned clean OSS image as the current account's avatar
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ProfileAssetInput" } } } }
      responses:
        "204": { description: bound }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [media]
      summary: Remove the current account's avatar binding
      responses: { "204": { description: removed }, "401": { $ref: "#/components/responses/Unauthorized" } }
  /me/profile/banner:
    put:
      tags: [media]
      summary: Bind an owned clean OSS image as the current account's profile banner
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ProfileAssetInput" } } } }
      responses:
        "204": { description: bound }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [media]
      summary: Remove the current account's profile banner binding
      responses: { "204": { description: removed }, "401": { $ref: "#/components/responses/Unauthorized" } }

  # ============ governance appeals and notices ============
  /me/appeals:
    get:
      tags: [identity, admin]
      summary: List my appeal cases and immutable status history
      description: Accepts either a normal account bearer or a short-lived appeal-only bearer.
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AppealPage" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [identity, admin]
      summary: Submit an appeal for my own active governance disposition
      description: One case per original event. The original action must be within 30 days and still active.
      parameters: [{ $ref: "#/components/parameters/AppealIdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/SubmitAppealInput" } } } }
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Appeal" } } } }
        "200": { description: idempotent replay, content: { application/json: { schema: { $ref: "#/components/schemas/Appeal" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /me/appeals/{id}/withdraw:
    post:
      tags: [identity, admin]
      summary: Withdraw my unclaimed appeal without erasing history
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AppealTransitionInput" } } } }
      responses:
        "200": { description: withdrawn, content: { application/json: { schema: { $ref: "#/components/schemas/Appeal" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /me/governance-notices:
    get:
      tags: [notifications]
      summary: List mandatory account-private governance notices
      description: Notices contain no staff identity, reporter identity, or private evidence.
      parameters:
        - { name: unread, in: query, schema: { type: boolean, default: false } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/GovernanceNoticePage" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/governance-notices/unread-count:
    get:
      tags: [notifications]
      summary: Count unread mandatory governance notices
      responses:
        "200": { description: ok, content: { application/json: { schema: { type: object, required: [count], properties: { count: { type: integer, minimum: 0 } } } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /me/governance-notices/read:
    post:
      tags: [notifications]
      summary: Mark selected or all governance notices read
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/GovernanceNoticeReadInput" } } } }
      responses:
        "204": { description: marked }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  # ============ notifications ============
  /notifications:
    get:
      tags: [notifications]
      summary: My notifications
      parameters: [{ name: unread, in: query, schema: { type: boolean } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationPage" } } } } }
  /notifications/read:
    post:
      tags: [notifications]
      summary: Mark notifications read
      description: >-
        Send ids to mark selected notifications or all=true to mark every notification. For rolling
        compatibility, an empty object still means mark all; new clients must send all=true explicitly.
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: "#/components/schemas/NotificationReadInput" } }
      responses:
        "204": { description: marked }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /notifications/unread-count:
    get: { tags: [notifications], summary: Unread notification count, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationUnreadCount" } } } } } }
  /notifications/stream:
    get:
      tags: [notifications]
      summary: SSE stream for real-time notifications
      description: >-
        Best-effort refresh hints only. The stream emits sync immediately after every connection;
        clients must refetch durable notification and unread-count endpoints after sync or any event.
      responses:
        "200":
          description: SSE stream
          content:
            text/event-stream: { schema: { type: string } }
  /me/notification-prefs:
    get: { tags: [notifications], summary: Get notification preferences, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationPrefs" } } } } } }
    put: { tags: [notifications], summary: Update notification preferences, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationPrefsInput" } } } }, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationPrefs" } } } } } }

  # ============ platform ============
  /startup/verify:
    post:
      tags: [platform]
      security: []
      summary: Captcha gate (Turnstile / TongjiCaptcha)
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [token], properties: { token: { type: string } } } } } }
      responses:
        "200": { description: ok }
        "400": { $ref: "#/components/responses/BadRequest" }
  /announcements:
    get:
      tags: [platform]
      security: [{ bearer: [] }, {}]
      summary: Active announcements applicable to the current viewer
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Announcement" }
  /announcements/unread:
    get:
      tags: [platform]
      summary: Active current announcement revisions not yet seen by the current account
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Announcement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /announcements/{id}/receipt:
    post:
      tags: [platform]
      summary: Record seen, dismissed, or acknowledged state for the current revision
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AnnouncementReceiptInput" } } } }
      responses:
        "200": { description: recorded, content: { application/json: { schema: { $ref: "#/components/schemas/AnnouncementReceipt" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /promotions:
    get:
      tags: [platform]
      security: [{ bearer: [] }, {}]
      summary: Active first-party promotions applicable to the current viewer
      parameters:
        - { name: placement, in: query, schema: { type: string, enum: [home-left-primary, home-left-secondary] } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Promotion" }
  /promotions/{id}/events:
    post:
      tags: [platform]
      security: []
      summary: Record one deduplicated anonymous promotion impression or click
      description: A click implicitly records the matching impression when it was not delivered first. The signed presentation token contains no account, IP, or device identifier.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PromotionEventInput" } } } }
      responses:
        "204": { description: recorded or already recorded }
        "400": { $ref: "#/components/responses/BadRequest" }
  /settings:
    get: { tags: [platform], security: [], summary: Public runtime settings, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Setting" } } } } } } }
  /onebox:
    get:
      tags: [platform]
      security: []
      summary: Onebox URL preview
      parameters: [{ name: url, in: query, required: true, schema: { type: string } }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/OneboxResult" } } } } }

  # ============ admin (role: mod/admin) ============
  /admin/overview:
    get:
      tags: [admin]
      summary: Administration dashboard counters
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AdminOverview" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/audit-events:
    get:
      tags: [admin]
      summary: Cursor-paginated immutable administration audit events
      parameters:
        - { name: actorId, in: query, schema: { type: string } }
        - { name: action, in: query, schema: { type: string } }
        - { name: targetType, in: query, schema: { type: string } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AdminAuditEventPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/notification-outbox:
    get:
      tags: [admin, notifications]
      summary: List durable notification outbox operations by state
      description: Requires operations.jobs. Payloads and source keys are intentionally excluded.
      parameters:
        - { name: state, in: query, schema: { type: string, enum: [queued, running, succeeded, dead, cancelled], default: dead } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationOutboxEventPage" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/notification-outbox/{id}/retry:
    post:
      tags: [admin, notifications]
      summary: Retry one dead-letter notification operation
      description: Requires operations.jobs. The reason and retry metadata are written to the immutable admin audit log.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationOutboxRetryInput" } } } }
      responses:
        "200": { description: queued, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationOutboxEvent" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/appeals:
    get:
      tags: [admin]
      summary: List lower-role appeal cases visible to an independent reviewer
      parameters:
        - { name: status, in: query, schema: { $ref: "#/components/schemas/AppealStatus" } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AdminAppealPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/appeals/{id}/review:
    post:
      tags: [admin]
      summary: Claim a submitted appeal for independent review
      description: The reviewer must outrank the appellant and cannot be the original disposition actor.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AppealTransitionInput" } } } }
      responses:
        "200": { description: in review, content: { application/json: { schema: { $ref: "#/components/schemas/AdminAppeal" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/appeals/{id}/decision:
    post:
      tags: [admin]
      summary: Decide an assigned appeal and atomically apply any owner-domain reversal
      description: Content targets support upheld/overturned. Sanctions additionally support a shortening amendment; unsupported adjustments fail closed.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AppealDecisionInput" } } } }
      responses:
        "200": { description: decided, content: { application/json: { schema: { $ref: "#/components/schemas/AdminAppeal" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit/reconciliations:
    get:
      tags: [admin, credit]
      summary: List persistent read-only credit reconciliation runs
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CreditReconciliationRunPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin, credit]
      summary: Request an idempotent read-only ledger and wallet projection reconciliation
      description: Verifies the append-only ledger before comparing every derived wallet balance. It never changes wallet balances or ledger rows.
      parameters: [{ $ref: "#/components/parameters/ReconciliationIdempotencyKey" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ReconciliationRunInput" } } } }
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/CreditReconciliationRun" } } } }
        "200": { description: idempotent replay, content: { application/json: { schema: { $ref: "#/components/schemas/CreditReconciliationRun" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/credit/reconciliations/stats:
    get:
      tags: [admin, credit]
      summary: Read aggregate credit reconciliation health counters
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CreditReconciliationStats" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/credit/reconciliations/{id}:
    get:
      tags: [admin, credit]
      summary: Read one credit reconciliation run
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CreditReconciliationRun" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/credit/reconciliations/{id}/resume:
    post:
      tags: [admin, credit]
      summary: Resume a queued or interrupted credit reconciliation run
      description: Target-state idempotent. A terminal run is returned unchanged and never executed again.
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ReconciliationRunInput" } } } }
      responses:
        "200": { description: current run state, content: { application/json: { schema: { $ref: "#/components/schemas/CreditReconciliationRun" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/credit/reconciliations/{id}/wallets:
    get:
      tags: [admin, credit]
      summary: Read per-wallet projection comparisons for one reconciliation run
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: driftOnly, in: query, schema: { type: boolean, default: true } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CreditReconciliationWalletPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/activity-policy:
    get:
      tags: [admin, activity]
      summary: Current activity scoring policy
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ActivityPolicy" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
    put:
      tags: [admin, activity]
      summary: Publish a new activity scoring policy revision
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ActivityPolicyUpdateInput" } } } }
      responses: { "200": { description: updated, content: { application/json: { schema: { $ref: "#/components/schemas/ActivityPolicy" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /admin/activity-policy/history:
    get:
      tags: [admin, activity]
      summary: Activity scoring policy revision history
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ActivityPolicyPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/trust-policy:
    get:
      tags: [admin, activity]
      summary: Current trust threshold policy
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/TrustLevelPolicy" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
    put:
      tags: [admin, activity]
      summary: Publish a new trust threshold policy revision
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/TrustLevelPolicyUpdateInput" } } } }
      responses: { "200": { description: updated, content: { application/json: { schema: { $ref: "#/components/schemas/TrustLevelPolicy" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /admin/trust-policy/history:
    get:
      tags: [admin, activity]
      summary: Trust threshold policy revision history
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/TrustLevelPolicyPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/users/{id}/trust-level:
    patch:
      tags: [admin, activity]
      summary: Manually set or clear a registered account trust override
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/TrustLevelAdjustInput" } } } }
      responses: { "200": { description: updated, content: { application/json: { schema: { $ref: "#/components/schemas/TrustProgress" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /admin/users/{id}/trust-events:
    get:
      tags: [admin, activity]
      summary: Append-only trust transition history for one account
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/TrustLevelEventPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /admin/users:
    get:
      tags: [admin]
      summary: Search and filter the administration user directory
      parameters:
        - { name: q, in: query, schema: { type: string } }
        - { name: role, in: query, schema: { type: string, enum: [user, mod, admin] } }
        - { name: status, in: query, schema: { type: string, enum: [active, suspended, deactivated, deletion_requested, deleted, purged] } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AdminUserPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
    post:
      tags: [admin]
      summary: Invite and provision a campus account
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminUserInviteInput" } } } }
      responses: { "201": { description: invited, content: { application/json: { schema: { $ref: "#/components/schemas/AdminUser" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /admin/account-lifecycle/jobs:
    get:
      tags: [admin]
      summary: Inspect durable account lifecycle jobs, including exhausted dead letters
      parameters:
        - { name: accountId, in: query, schema: { type: string } }
        - { name: jobType, in: query, schema: { type: string, enum: [mark_deleted, purge] } }
        - { name: status, in: query, schema: { type: string, enum: [queued, running, succeeded, failed] } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: lifecycle job page, content: { application/json: { schema: { $ref: "#/components/schemas/AdminLifecycleJobPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
  /admin/account-lifecycle/jobs/{id}/requeue:
    post:
      tags: [admin]
      summary: Requeue one failed account lifecycle job without reopening account recovery
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }
      responses:
        "200": { description: requeued lifecycle job, content: { application/json: { schema: { $ref: "#/components/schemas/AdminLifecycleJob" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/users/{id}/role:
    patch:
      tags: [admin]
      summary: Change a user's platform role
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminUserRoleInput" } } } }
      responses: { "200": { description: updated, content: { application/json: { schema: { $ref: "#/components/schemas/AdminUser" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" }, "428": { $ref: "#/components/responses/RecentAuthRequired" } }
  /admin/users/{id}/sessions/revoke:
    post:
      tags: [admin]
      summary: Revoke every active session for a user
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }
      responses: { "204": { description: revoked }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "428": { $ref: "#/components/responses/RecentAuthRequired" } }
  /admin/reviews:
    get:
      tags: [admin]
      summary: List reviews for moderation
      parameters: [{ name: status, in: query, schema: { type: string, enum: [visible, hidden, pending, all] } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ReviewPage" } } } } }
  /admin/reviews/{id}:
    delete:
      tags: [admin]
      summary: Delete a review
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }
      responses: { "204": { description: ok } }
  /admin/reviews/{id}/toggle:
    post:
      tags: [admin]
      summary: Toggle visibility (hide/unhide)
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }
      responses: { "200": { description: ok, content: { application/json: { schema: { type: object, required: [ok], properties: { ok: { type: boolean } } } } } } }
  /admin/courses:
    get:
      tags: [admin]
      summary: List courses
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/CoursePage" } } } } }
    post:
      tags: [admin]
      summary: Create a course
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminCourseCreateInput" } } } }
      responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Course" } } } } }
  /admin/courses/{id}:
    put:
      tags: [admin]
      summary: Update a course
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminCourseUpdateInput" } } } }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Course" } } } } }
    delete:
      tags: [admin]
      summary: Delete a course
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }
      responses: { "204": { description: ok } }
  /admin/reports:
    get:
      tags: [admin]
      summary: Report queue
      parameters: [{ name: status, in: query, schema: { type: string, enum: [open, upheld, rejected, ignored, all] } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ReportPage" } } } } }
  /admin/reports/{id}/resolve:
    post:
      tags: [admin]
      summary: Resolve a report
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/ReviewReportResolutionInput" } } } }
      responses: { "200": { description: resolved, content: { application/json: { schema: { $ref: "#/components/schemas/Report" } } } } }
  /admin/dm/reports:
    get:
      tags: [admin]
      summary: Private-message report queue; unreported conversations are never exposed
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [open, upheld, rejected], default: open } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/DmReportPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/dm/reports/{id}/resolve:
    post:
      tags: [admin]
      summary: Resolve a private-message report
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/DmReportResolutionInput" } } } }
      responses: { "200": { description: resolved, content: { application/json: { schema: { $ref: "#/components/schemas/DmReport" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /admin/settings:
    get: { tags: [admin], summary: All settings, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Setting" } } } } } } }
  /admin/settings/{key}:
    put:
      tags: [admin]
      summary: Update a setting
      parameters: [{ name: key, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/SettingUpdateInput" } } } }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Setting" } } } } }
  /admin/announcements:
    get:
      tags: [admin]
      summary: List announcements for administration
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AnnouncementPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
    post:
      tags: [admin]
      summary: Create a draft, scheduled, or published announcement
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AnnouncementCreateInput" } } } }
      responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Announcement" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/announcements/{id}:
    patch:
      tags: [admin]
      summary: Update announcement copy, audience, presentation, or scheduling with optimistic concurrency
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AnnouncementUpdateInput" } } } }
      responses: { "200": { description: updated, content: { application/json: { schema: { $ref: "#/components/schemas/Announcement" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
    delete:
      tags: [admin]
      summary: Archive an announcement while retaining revisions and receipts
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminVersionedArchiveInput" } } } }
      responses: { "204": { description: archived }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /admin/announcements/{id}/revisions:
    get:
      tags: [admin]
      summary: Immutable announcement mutation history
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AnnouncementRevisionPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /admin/promotions:
    get:
      tags: [admin]
      summary: List first-party promotions for administration
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/PromotionPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
    post:
      tags: [admin]
      summary: Create a first-party promotion with an optional clean platform asset reference
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PromotionCreateInput" } } } }
      responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Promotion" } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/promotions/{id}:
    patch:
      tags: [admin]
      summary: Update a promotion with optimistic concurrency
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/PromotionUpdateInput" } } } }
      responses: { "200": { description: updated, content: { application/json: { schema: { $ref: "#/components/schemas/Promotion" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
    delete:
      tags: [admin]
      summary: Archive a promotion
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminVersionedArchiveInput" } } } }
      responses: { "204": { description: archived }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /admin/promotions/{id}/metrics:
    get:
      tags: [admin]
      summary: Read privacy-minimal daily promotion delivery metrics
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: from, in: query, schema: { type: string, format: date }, description: Inclusive UTC date; defaults to 29 days before today. }
        - { name: to, in: query, schema: { type: string, format: date }, description: Inclusive UTC date; defaults to today and cannot be in the future. }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/PromotionMetrics" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/media/uploads:
    get:
      tags: [admin, media]
      summary: Pending media moderation queue
      parameters:
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
        - { name: status, in: query, schema: { type: string, enum: [pending, clean, quarantined, blocked], default: pending }, description: Moderation state selected before hierarchy-safe pagination. }
      responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/UploadPage" } } } }, "403": { $ref: "#/components/responses/Forbidden" } }
  /admin/media/retention-holds:
    get:
      tags: [admin, media]
      summary: List detailed media retention holds by nearest expiry
      description: Requires operations.jobs and a recent-authenticated revocable session. This purpose-bearing inventory is unavailable to ordinary moderators and is audited on every read.
      parameters:
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
        - { name: state, in: query, schema: { type: string, enum: [active, expired], default: active }, description: Unreleased holds are split by whether expiresAt is in the future. }
      responses:
        "200": { description: holds ordered by expiresAt then id, content: { application/json: { schema: { $ref: "#/components/schemas/MediaRetentionHoldPage" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/media/deletion-jobs:
    get:
      tags: [admin, media]
      summary: List durable system-owned media deletion jobs
      description: Requires operations.jobs and a recent-authenticated revocable session. Moderation-owned deletion work is excluded so system jobs remain operable independently from target role hierarchy; every inventory read is audited.
      parameters:
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
        - { name: status, in: query, schema: { type: string, enum: [queued, leased, succeeded, dead_letter], default: dead_letter } }
      responses:
        "200": { description: jobs ordered newest first, content: { application/json: { schema: { $ref: "#/components/schemas/MediaDeletionJobPage" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/media/reconciliation:
    get:
      tags: [admin, media]
      summary: Inspect bounded media database-state drift without mutation
      description: Requires operations.jobs and a recent-authenticated revocable session. Each read is audited. The report checks PostgreSQL moderation, publication, processing, and cleanup invariants only; OSS Ingest and Delivery object inventory remains an explicit manual provider-side reconciliation step.
      parameters:
        - name: cursor
          in: query
          schema: { type: string, pattern: '^[0-9]+$' }
          description: Asset-id cursor returned as nextCursor by the previous page.
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200":
          description: Read-only dry-run report ordered by ascending asset id
          headers:
            Cache-Control: { schema: { type: string }, description: "Always private, no-store." }
            Pragma: { schema: { type: string }, description: Always no-cache. }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MediaReconciliationReport" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/media/deletion-jobs/{id}/retry:
    post:
      tags: [admin, media]
      summary: Requeue one dead-lettered system media deletion job
      description: Requires operations.jobs and recent authentication. Only quarantined, system-owned dead letters can be retried; the operator's reason is stored separately from the job's immutable business purpose.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }
      responses:
        "202": { description: job requeued }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/media/uploads/{id}/preview-grants:
    post:
      tags: [admin, media]
      summary: Issue a one-time same-origin media preview grant
      description: Requires moderation.content. The moderator must be independent and higher-role, except that ADMIN may explicitly confirm own-media self-review with recent authentication. No provider identifier or URL is returned.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MediaModerationInput" } } } }
      responses:
        "200": { description: one-time grant, headers: { Cache-Control: { schema: { type: string } } }, content: { application/json: { schema: { $ref: "#/components/schemas/ModerationPreviewGrant" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/media/uploads/{id}/preview:
    get:
      tags: [admin, media]
      summary: Consume a one-time grant and proxy bounded image evidence
      description: Requires moderation.content and the same moderator who created the unexpired grant. The response is same-origin, no-store, MIME checked, bounded to 20 MiB / 20,000 px per side / 40 MP before the first byte, and audited without persisting a URL or key.
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: X-Media-Preview-Token, in: header, required: true, schema: { type: string, minLength: 43, maxLength: 43 } }
      responses:
        "200": { description: bounded static image stream, headers: { Cache-Control: { schema: { type: string } }, X-Content-Type-Options: { schema: { type: string } } }, content: { image/jpeg: { schema: { type: string, format: binary } }, image/png: { schema: { type: string, format: binary } }, image/webp: { schema: { type: string, format: binary } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/media/uploads/{id}/approve:
    post:
      tags: [admin, media]
      summary: Approve an evidence-backed pending image
      description: Requires strict role hierarchy and a trusted image preview completed by the same moderator. ADMIN own-media self-review additionally requires explicit confirmation and recent authentication. Approval queues sanitized Delivery processing; it does not make the original public. Generic files cannot be approved until malware and sandbox scanner evidence exists.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MediaModerationInput" } } } }
      responses: { "200": { description: approved }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" }, "428": { $ref: "#/components/responses/RecentAuthRequired" } }
  /admin/media/uploads/{id}/block:
    post:
      tags: [admin, media]
      summary: Quarantine an upload and enqueue durable provider deletion
      description: Supports pending and already-published clean uploads under strict role hierarchy, plus the explicit recent-authenticated ADMIN own-media exception. The database stops new delivery before provider I/O; durable cleanup purges CDN, deletes every Delivery variant, then deletes the Ingest original unless a retention hold pauses that final step.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MediaModerationInput" } } } }
      responses: { "202": { description: quarantined and deletion queued }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" }, "428": { $ref: "#/components/responses/RecentAuthRequired" } }
  /admin/media/uploads/{id}/processing/retry:
    post:
      tags: [admin, media]
      summary: Requeue one failed sanitized-variant processing job
      description: Requires operations.jobs and recent authentication. Only a clean asset whose current policy publication and durable job are failed/dead-lettered may be requeued; the reason and transition are audited atomically.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }
      responses:
        "202": { description: processing requeued }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/media/uploads/{id}/retention-hold:
    post:
      tags: [admin, media]
      summary: Place a purpose-bound, time-bounded hold that pauses provider deletion
      description: Requires operations.jobs and a recent-authenticated revocable session. Fails once provider deletion is leased or complete. expectedHoldId provides compare-and-set semantics so a reviewed hold can be renewed without an unprotected gap or overwriting concurrent work.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MediaRetentionHoldInput" } } } }
      responses:
        "201": { description: hold placed }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
    delete:
      tags: [admin, media]
      summary: Release the current media retention hold
      description: Requires operations.jobs and recent authentication. The release reason and bounded hold metadata are append-only audited.
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MediaRetentionHoldReleaseInput" } } } }
      responses:
        "204": { description: hold released }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "428": { $ref: "#/components/responses/RecentAuthRequired" }
  /admin/selection/sync:
    post: { tags: [admin], summary: Trigger 一系统 sync job, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }, responses: { "202": { description: queued } } }
  /admin/courses/reindex:
    post: { tags: [admin], summary: Rebuild the Meilisearch course documents, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }, responses: { "202": { description: queued } } }
  /admin/reviews/reindex:
    post: { tags: [admin], summary: Rebuild the Meilisearch review index, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }, responses: { "202": { description: queued } } }
  /admin/forum/reindex:
    post: { tags: [admin], summary: "Rebuild thread, public user, board, and tag search indices", requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }, responses: { "202": { description: queued } } }
  /admin/forum/boards:
    post: { tags: [admin], summary: Create board, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminBoardCreateInput" } } } }, responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Board" } } } } } }
  /admin/forum/boards/{id}:
    patch: { tags: [admin], summary: Update board, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminBoardUpdateInput" } } } }, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Board" } } } } } }
    delete: { tags: [admin], summary: Delete board, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }, responses: { "200": { description: deleted } } }
  /admin/forum/tags:
    get: { tags: [admin], summary: List tags, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Tag" } } } } } } }
    post: { tags: [admin], summary: Create tag, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminTagCreateInput" } } } }, responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Tag" } } } } } }
  /admin/forum/tags/{id}:
    patch: { tags: [admin], summary: Update tag, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminTagUpdateInput" } } } }, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Tag" } } } } } }
    delete: { tags: [admin], summary: Delete tag, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }, responses: { "200": { description: deleted } } }
  /admin/forum/threads/{id}:
    get: { tags: [admin], summary: Read a thread for moderation, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/ThreadDetail" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
  /admin/forum/comments/{id}:
    get: { tags: [admin], summary: Read a comment for moderation, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/Comment" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
  /admin/forum/threads/{id}/{action}:
    post:
      tags: [admin]
      summary: Apply a reasoned moderation or organization action to a thread
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: action, in: path, required: true, schema: { type: string, enum: [pin, unpin, close, reopen, archive, unarchive, delete, restore, hide, unhide, move] } }
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminThreadActionInput" } } } }
      responses: { "200": { description: updated }, "400": { $ref: "#/components/responses/BadRequest" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /admin/forum/comments/{id}/{action}:
    post:
      tags: [admin]
      summary: Apply a reasoned moderation action to a comment
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: action, in: path, required: true, schema: { type: string, enum: [delete, restore, hide, unhide] } }
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminCommentActionInput" } } } }
      responses: { "200": { description: updated }, "400": { $ref: "#/components/responses/BadRequest" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /admin/forum/threads/{id}/feature:
    post: { tags: [admin], summary: Feature or unfeature a thread, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FeatureThreadInput" } } } }, responses: { "200": { description: ok } } }
  /admin/forum/flags:
    get: { tags: [admin], summary: Flag queue, parameters: [{ name: status, in: query, schema: { type: string, enum: [open, upheld, rejected, ignored, all], default: open } }, { $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }], responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AdminForumFlagPage" } } } } } }
  /admin/forum/flags/{id}/resolve:
    post: { tags: [admin], summary: Resolve flag, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FlagResolveInput" } } } }, responses: { "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AdminForumFlag" } } } } } }
  /admin/forum/watched-words:
    get: { tags: [admin], summary: List watched words, responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/WatchedWord" } } } } } } }
    post: { tags: [admin], summary: Add watched word, requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/WatchedWordInput" } } } }, responses: { "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/WatchedWord" } } } } } }
  /admin/forum/watched-words/{id}:
    delete: { tags: [admin], summary: Delete watched word, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AdminReasonInput" } } } }, responses: { "200": { description: deleted } } }
  /admin/forum/mod-actions:
    get: { tags: [admin], summary: Staff action log, parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }], responses: { "200": { description: ok } } }
  /admin/users/{id}/silence:
    post: { tags: [admin], summary: Silence user, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/SanctionInput" } } } }, responses: { "204": { description: silenced }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
  /admin/users/{id}/suspend:
    post: { tags: [admin], summary: Suspend user, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/SanctionInput" } } } }, responses: { "204": { description: suspended }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "428": { $ref: "#/components/responses/RecentAuthRequired" } } }
  /admin/users/{id}/unsanction:
    post: { tags: [admin], summary: Revoke sanction, description: Revoking a suspension requires recent authentication; revoking an ordinary silence does not., parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UnsanctionInput" } } } }, responses: { "204": { description: revoked }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" }, "428": { $ref: "#/components/responses/RecentAuthRequired" } } }
  /admin/users/{id}/sanctions:
    get: { tags: [admin], summary: List sanctions for user, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { "200": { description: ok, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/Sanction" } } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
  /admin/achievements:
    get:
      tags: [admin, platform]
      summary: List versioned contribution achievement definitions
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin, platform]
      summary: Create a controlled contribution achievement definition
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementCreateInput" } } } }
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Achievement" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/achievements/{achievementId}:
    patch:
      tags: [admin, platform]
      summary: Update or retire an achievement with optimistic concurrency
      parameters: [{ name: achievementId, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementUpdateInput" } } } }
      responses:
        "200": { description: updated, content: { application/json: { schema: { $ref: "#/components/schemas/Achievement" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/users/{id}/achievements:
    get:
      tags: [admin, platform]
      summary: List an account's active and revoked contribution achievements
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementGrantPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [admin, platform]
      summary: Manually award or re-award an achievement without minting points
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementGrantInput" } } } }
      responses:
        "201": { description: awarded, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementGrant" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/users/{id}/achievements/{achievementId}/revoke:
    post:
      tags: [admin, platform]
      summary: Revoke an account achievement without reversing historical contribution points
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: achievementId, in: path, required: true, schema: { type: string } }
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementRevokeInput" } } } }
      responses:
        "200": { description: revoked, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementGrant" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/users/{id}/achievement-events:
    get:
      tags: [admin, platform]
      summary: List append-only achievement award and revocation history
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementEventPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /admin/platform/badges:
    get:
      deprecated: true
      tags: [admin, platform]
      summary: Deprecated alias for listing contribution achievements
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementPage" } } } }
    post:
      deprecated: true
      tags: [admin, platform]
      summary: Deprecated alias for creating a contribution achievement
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/AchievementCreateInput" } } } }
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/Achievement" } } } }
  /admin/verifications/types:
    get:
      tags: [admin, platform]
      summary: List staff-managed identity and special verification definitions
      parameters: [{ $ref: "#/components/parameters/Cursor" }, { $ref: "#/components/parameters/Limit" }]
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationTypePage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin, platform]
      summary: Create a typed verification definition
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationTypeInput" } } } }
      responses:
        "201": { description: created, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationType" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/users/{id}/verifications:
    get:
      tags: [admin, platform]
      summary: List an account's verification grant history
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { $ref: "#/components/parameters/Cursor" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200": { description: ok, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationGrantPage" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [admin, platform]
      summary: Grant an identity or special verification to a lower-role account
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationGrantInput" } } } }
      responses:
        "201": { description: granted, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationGrant" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /admin/verifications/grants/{id}/revoke:
    post:
      tags: [admin, platform]
      summary: Revoke an active verification grant without erasing its history
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationRevokeInput" } } } }
      responses:
        "200": { description: revoked, content: { application/json: { schema: { $ref: "#/components/schemas/VerificationGrant" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
