# web

React v2 frontend (placeholder). Scaffolding lands in a later step; it consumes
the generated client from `../contract/openapi.yaml`. Keep all API types generated
— do not hand-write request/response shapes.
